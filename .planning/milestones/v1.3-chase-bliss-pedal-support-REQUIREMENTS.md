---
milestone: v1.3
archived: 2026-06-10
status: all_satisfied
---

# Requirements — v1.3 Chase Bliss Pedal Support (Archived)

**Milestone:** v1.3 Chase Bliss Pedal Support
**Status:** All satisfied
**Archived:** 2026-06-10

---

## Milestone Goal

Add full MIDI CC catalog, preset validation, and apply integration for Mood MkII (gap-fill), Wombtone MkII, and Brothers AM Chase Bliss pedals, including a reset-to-defaults step in the apply flow for clean preset creation.

---

## Active Requirements (Archived)

### Catalog

- [x] **CBA-01**: Wombtone MkII MIDI CC catalog — all 8 parameter controls (CC14-21) with CC numbers, ranges, positions, and default values
- [x] **CBA-02**: Brothers AM MIDI CC catalog — all 24 parameter controls (8 knobs, 3 toggles, 15 dip switches; CC14-77) with CC numbers, ranges, positions, and default values
- [x] **CBA-03**: Mood MkII catalog backfill — add 10 missing controls (CC24-29 hidden options, CC31-33 sync/spread/buffer, CC52 stop-ramping), fix CC102/103 name swap (wet_bypass/loop_bypass are reversed), and add `default=` values to all existing controls
- [x] **CBA-04**: `Control` model gains optional `default: float | None = None` field; footswitches and global/utility CCs always get `default=None` to exclude them from reset

### Validation

- [x] **CBA-05**: When applying a Chase Bliss preset, parameter names are validated against the device's catalog controls (unknown name → `ValidationError` with helpful message); parameter values are validated against `control.min`/`control.max` range (out-of-range → `ValidationError`); validation runs before prompting the user to navigate to the preset slot on the pedal, and always runs including in dry-run mode

### Reset

- [x] **CBA-06**: Before sending a preset's CC parameter values during apply Phase 2, all catalog controls with a non-None default are sent to their default CC value (reset-to-defaults); footswitches and utility CCs (`default=None`) are excluded from the reset batch; reset fires per-preset immediately before preset CC messages

---

## Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| CBA-04 | Phase 14 | [x] Satisfied |
| CBA-01 | Phase 14 + Phase 18 | [x] Satisfied |
| CBA-02 | Phase 14 + Phase 18 | [x] Satisfied |
| CBA-03 | Phase 14 + Phase 17 | [x] Satisfied |
| CBA-05 | Phase 15 | [x] Satisfied |
| CBA-06 | Phase 16 | [x] Satisfied |

---

## Deferred

| Requirement | Reason Deferred |
|-------------|-----------------|
| Synth Mode controls (Mood MkII CC57-84) | Saved globally, not per-preset; separate workflow concern |
| CBA pedals beyond these three | Add when hardware is available for testing |
| Plugin authoring docs (PKG-07) | Nice-to-have; not blocking pedal support |

---

## Requirement Outcomes

| REQ-ID | Outcome | Notes |
|--------|---------|-------|
| CBA-04 | Validated | Control.default field implemented and consumed by Phases 15 and 16 |
| CBA-01 | Validated | Wombtone MkII catalog completed and wired via Phase 18 `from_raw_yaml()` |
| CBA-02 | Validated | Brothers AM catalog completed and wired via Phase 18 `from_raw_yaml()` |
| CBA-03 | Validated | Mood MkII catalog backfilled; stale tests fixed in Phase 17 |
| CBA-05 | Validated | Preset parameter validation implemented; runs before pedal interaction in both dry-run and live paths |
| CBA-06 | Validated | Reset-to-defaults implemented; per-preset execution with non-None default filter |

---

_Archived from .planning/REQUIREMENTS.md at v1.3 milestone completion_
