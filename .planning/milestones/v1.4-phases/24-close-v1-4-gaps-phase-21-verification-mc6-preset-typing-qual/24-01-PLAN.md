---
phase: 24-close-v1-4-gaps-phase-21-verification-mc6-preset-typing-qual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rig-morningstar/src/rig_morningstar/device.py
  - packages/rig/src/rig/engine/plan/models.py
  - packages/rig/src/rig/engine/plan/compute.py
  - packages/rig/src/rig/engine/apply.py
  - packages/rig/src/rig/cli/commands/plan.py
  - packages/rig/tests/test_devices.py
  - .planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TYPE-02, TYPE-03, QUAL-01]

must_haves:
  truths:
    - "MC6Device.presets is typed list[Preset], satisfying the Preset Protocol — no list[Any] remains in any plugin device class"
    - "DeviceAction.device_type is typed DeviceType (enum), not str"
    - "DeviceAction.status is typed ActionStatus (StrEnum), not a raw Literal"
    - "All DeviceAction construction sites pass enum members, not raw strings"
    - "Phase 21 VERIFICATION.md exists and records live grep output for all 4 Phase 21 success criteria"
    - "REQUIREMENTS.md marks TYPE-02 and TYPE-03 as complete in both the checklist and the Traceability table"
    - "make test passes with no new regressions"
  artifacts:
    - path: "packages/rig/src/rig/engine/plan/models.py"
      provides: "ActionStatus StrEnum; DeviceAction typed with DeviceType and ActionStatus"
      contains: "class ActionStatus"
    - path: "packages/rig-morningstar/src/rig_morningstar/device.py"
      provides: "MC6Device.presets typed as list[Preset]"
      contains: "list[Preset]"
    - path: ".planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md"
      provides: "Phase 21 verification document with live evidence"
      contains: "TYPE-02"
  key_links:
    - from: "packages/rig/src/rig/engine/plan/compute.py"
      to: "ActionStatus / DeviceType enum members"
      via: "DeviceAction construction"
      pattern: "ActionStatus\\.|DeviceType\\."
    - from: "packages/rig-morningstar/src/rig_morningstar/device.py"
      to: "rig.engine.plugin.Preset"
      via: "import + list[Preset] annotation"
      pattern: "from rig.engine.plugin import.*Preset"
---

<objective>
Close the three remaining v1.4 audit gaps: (1) MC6Device.presets carries the concrete `Preset` type so the Morningstar plugin fully satisfies the `Preset` Protocol (D-01, TYPE-03); (2) `DeviceAction` uses enum types throughout — `device_type: DeviceType` (D-02, D-03) and a new `status: ActionStatus` StrEnum (D-04, D-05), closing the QUAL-01 assignment sites; (3) write the missing Phase 21 VERIFICATION.md (D-06) and mark TYPE-02/TYPE-03 complete in REQUIREMENTS.md (D-07).

Purpose: The v1.4 milestone audit flagged these as open gaps. Closing them makes every plugin device class type-clean against the engine Protocol and removes the last raw device-type strings from plan/apply construction.

Output: Typed MC6 presets, enum-typed DeviceAction, Phase 21 VERIFICATION.md, updated REQUIREMENTS.md. Zero user-visible behavior changes (StrEnum serializes to its `.value` automatically).
</objective>

<artifacts_produced>
## Artifacts this phase produces

New symbols and files created by this phase (exclude from drift verification):

- `ActionStatus` — new `StrEnum` class in `packages/rig/src/rig/engine/plan/models.py` with members `CONFIGURE = "configure"`, `VERIFY = "verify"`, `ANALOG = "analog"`
- `.planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md` — new file

Modified (not new) symbols:
- `DeviceAction.device_type` — retyped `str` → `DeviceType`
- `DeviceAction.status` — retyped `Literal["configure","verify","analog"]` → `ActionStatus`
- `MC6Device.presets` — retyped `list[Any]` → `list[Preset]`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-close-v1-4-gaps-phase-21-verification-mc6-preset-typing-qual/24-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-quick-wins-dead-code/20-01-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ActionStatus enum and retype DeviceAction + MC6 presets</name>
  <files>packages/rig/src/rig/engine/plan/models.py, packages/rig-morningstar/src/rig_morningstar/device.py</files>
  <read_first>
    - packages/rig/src/rig/engine/plan/models.py (current DeviceAction definition — device_type: str, status: Literal[...])
    - packages/rig/src/rig/engine/plugin.py lines 47-51 (DeviceType StrEnum — exact pattern to copy for ActionStatus) and lines 115-122 (Preset Protocol)
    - packages/rig-morningstar/src/rig_morningstar/device.py (current presets: list[Any] = Field(default_factory=list); existing import from rig.engine.plugin)
    - packages/rig-analog/src/rig_analog/device.py lines 32-34 (reference: a plugin device that already uses concrete config + typed presets list)
  </read_first>
  <behavior>
    - DeviceAction(device="x", device_type=DeviceType.ANALOG, status=ActionStatus.ANALOG) constructs without error
    - DeviceAction(device="x", device_type=DeviceType.DIGITAL, status=ActionStatus.CONFIGURE).model_dump_json() emits "device_type":"digital" and "status":"configure" (StrEnum serializes to .value — JSON output unchanged from before)
    - ActionStatus.CONFIGURE == "configure" is True (StrEnum equality preserved, so existing string comparisons keep working)
    - MC6Device(id="mc6", config=MC6Config()) has presets == [] and MC6Device.model_fields["presets"] annotation resolves to list[Preset]
  </behavior>
  <action>
    In packages/rig/src/rig/engine/plan/models.py: add `from enum import StrEnum` import and `from rig.engine.plugin import DeviceType` import. Define `class ActionStatus(StrEnum)` with exactly three members: `CONFIGURE = "configure"`, `VERIFY = "verify"`, `ANALOG = "analog"` (per D-04, parallel pattern to DeviceType at plugin.py:47). Place ActionStatus above the DeviceAction class. Change `DeviceAction.device_type: str` → `device_type: DeviceType` (D-02). Change `DeviceAction.status: Literal["configure", "verify", "analog"]` → `status: ActionStatus` (D-05). Leave the `Literal` import in place only if still used by ScenePlan/Plan (it is — ScenePlan.status and Plan.status keep their Literal types; do not touch those).
    In packages/rig-morningstar/src/rig_morningstar/device.py: add `Preset` to the existing `from rig.engine.plugin import (...)` block (D-01). Change `presets: list[Any] = Field(default_factory=list)` → `presets: list[Preset] = Field(default_factory=list)`. Leave `from_raw_yaml` returning `presets=[]` unchanged — MC6 never carries real presets.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run python -c "from rig.engine.plan.models import ActionStatus, DeviceAction; from rig.engine.plugin import DeviceType; a=DeviceAction(device='x', device_type=DeviceType.DIGITAL, status=ActionStatus.CONFIGURE); import json; d=json.loads(a.model_dump_json()); assert d['device_type']=='digital' and d['status']=='configure', d; assert ActionStatus.CONFIGURE=='configure'; from rig_morningstar.device import MC6Device; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ActionStatus(StrEnum)" packages/rig/src/rig/engine/plan/models.py` returns one hit
    - `grep -n "device_type: DeviceType" packages/rig/src/rig/engine/plan/models.py` returns one hit; `grep -n "device_type: str" packages/rig/src/rig/engine/plan/models.py` returns zero hits
    - `grep -n "status: ActionStatus" packages/rig/src/rig/engine/plan/models.py` returns one hit
    - `grep -c "presets: list\[Any\]" packages/rig-morningstar/src/rig_morningstar/device.py` returns 0; `grep -c "presets: list\[Preset\]" packages/rig-morningstar/src/rig_morningstar/device.py` returns 1
    - The verify command prints `OK` (construction succeeds and JSON serialization is unchanged)
  </acceptance_criteria>
  <done>ActionStatus StrEnum defined; DeviceAction.device_type is DeviceType; DeviceAction.status is ActionStatus; MC6Device.presets is list[Preset]; JSON serialization unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update DeviceAction construction sites and consumers to enum members</name>
  <files>packages/rig/src/rig/engine/plan/compute.py, packages/rig/src/rig/engine/apply.py, packages/rig/src/rig/cli/commands/plan.py, packages/rig/tests/test_devices.py</files>
  <read_first>
    - packages/rig/src/rig/engine/plan/compute.py lines 89-126 (the two DeviceAction construction sites: analog at ~93-95, configure/verify at ~117-119; note device_type=pedal.type.value at line 118)
    - packages/rig/src/rig/engine/apply.py lines 160-168 (controller DeviceAction construction site: device_type="controller", status="configure")
    - packages/rig/src/rig/cli/commands/plan.py lines 97-114 (consumers: action.device_type == DeviceType.ANALOG already; action.status == "configure" and == "verify" raw-string comparisons)
    - packages/rig/tests/test_devices.py lines 42-49 (test builder constructing DeviceAction with device_type="analog", status="analog")
    - packages/rig/src/rig/engine/plan/models.py (the new ActionStatus enum and retyped fields from Task 1)
  </read_first>
  <action>
    In compute.py: the analog site (~line 93-95) — change `device_type="analog"` → `device_type=DeviceType.ANALOG` and `status="analog"` → `status=ActionStatus.ANALOG` (D-03). The configure/verify site (~line 118-119) — change `device_type=pedal.type.value` → `device_type=pedal.type` (pass the enum member directly now that the field is typed, per D-03) and `status="configure" if needs_config else "verify"` → `status=ActionStatus.CONFIGURE if needs_config else ActionStatus.VERIFY`. Add `ActionStatus` to the existing `from rig.engine.plan.models import ...` import; DeviceType is already imported. Do NOT change ScenePlan scene_status (it stays a Literal).
    In apply.py: the controller site (~line 163-167) — change `device_type="controller"` → `device_type=DeviceType.CONTROLLER` and `status="configure"` → `status=ActionStatus.CONFIGURE` (D-03). Add an import: `from rig.engine.plugin import DeviceType` (apply.py currently imports DeviceApplyContext/DeviceApplyResult/SetupContext from there — add DeviceType to that line) and import `ActionStatus` from `rig.engine.plan.models` (the file already does a local `from rig.engine.plan.models import DeviceAction` near line 161 — add ActionStatus there or hoist to module top).
    In cli/commands/plan.py: change `action.status == "configure"` → `action.status == ActionStatus.CONFIGURE` and `action.status == "verify"` → `action.status == ActionStatus.VERIFY` (QUAL-01 — close the raw-string comparison sites). Add `ActionStatus` to the import from `rig.engine.plan.models`. Leave `action.device_type == DeviceType.ANALOG` as-is (already an enum comparison) and leave `sp.status == "unchanged"/"new"/"changed"` as-is (ScenePlan.status is still a Literal).
    In tests/test_devices.py: change the `_make_ctx` builder's DeviceAction (line ~42-45) — `device_type="analog"` → `device_type=DeviceType.ANALOG`, `status="analog"` → `status=ActionStatus.ANALOG`. Add the needed imports (`from rig.engine.plugin import DeviceType`, `from rig.engine.plan.models import ActionStatus` — or extend existing imports).
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig/tests/test_plan.py packages/rig/tests/test_devices.py packages/rig/tests/test_cli_plan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'device_type="' packages/rig/src/rig/engine/ packages/rig/src/rig/cli/` returns zero hits (no raw-string device_type assignments remain)
    - `grep -rn 'status="configure"\|status="verify"\|status="analog"' packages/rig/src/rig/engine/` returns zero hits
    - `grep -rn 'device_type=pedal.type.value' packages/rig/src/rig/engine/plan/compute.py` returns zero hits
    - `grep -c 'action.status == "configure"\|action.status == "verify"' packages/rig/src/rig/cli/commands/plan.py` returns 0
    - `uv run pytest packages/rig/tests/test_plan.py packages/rig/tests/test_devices.py packages/rig/tests/test_cli_plan.py -q` passes
  </acceptance_criteria>
  <done>All 4 DeviceAction construction sites use enum members; plan.py status comparisons use ActionStatus members; test builder uses enum members; plan/devices/cli_plan tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 21 VERIFICATION.md and update REQUIREMENTS.md</name>
  <files>.planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/20-quick-wins-dead-code/20-01-VERIFICATION.md (style/format to match: frontmatter, Phase Goal, Truth Checks table with live grep evidence, Requirements Satisfied)
    - .planning/phases/21-concrete-types-plugin-boundary/21-01-PLAN.md (Phase 21 success criteria being verified)
    - .planning/REQUIREMENTS.md lines 12-16 (TYPE-02, TYPE-03 checklist entries) and lines 52-64 (Traceability table)
    - packages/rig/src/rig/engine/plugin.py lines 115-122 (Preset Protocol — evidence for criterion 2)
  </read_first>
  <action>
    Run these four live checks (after Tasks 1-2 land) and record their ACTUAL output in the VERIFICATION.md Truth Checks table — do not assert PASS without running them:
    (1) Criterion 1 (no `config: Any` in plugin source): `grep -rn "config: Any" packages/*/src/` — expect zero hits in real code. Note explicitly that the only `config: Any` text lives inside the plugin.py docstring example (Plugin Authoring Contract), which is documentation, not a device-class annotation; the grep above against `packages/*/src/` will surface that docstring line, so scope the evidence to plugin device classes (`grep -rn "config: " packages/*/src/*/device.py` shows all four are concrete: AnalogConfig, ChaseBlissConfig, HXStompConfig, MC6Config).
    (2) Criterion 2 (Preset Protocol in core): `grep -rn "class Preset(Protocol)" packages/rig/src/rig/engine/plugin.py` — expect one hit at line ~115.
    (3) Criterion 3 (presets: list[Preset] in all plugins, no list[Any]): `grep -rn "presets: list\[Any\]" packages/*/src/*/device.py` — expect zero hits; and `grep -rn "presets:" packages/*/src/*/device.py` — expect all four device classes typed (AnalogPreset, HXStompPreset|MidiPreset, DigitalPreset, Preset).
    (4) Criterion 4 (YAML invalid field raises validation error, not AttributeError): construct an MC6Config (or any plugin config) with an invalid field and confirm a Pydantic ValidationError — run `uv run python -c "from rig_morningstar.config import MC6Config; MC6Config(bogus_field=1)"` and record that it raises ValidationError (note: only if MC6Config forbids extra; if it allows extra, cite a config that forbids extra such as one used by another plugin, or cite the Phase 21 PLAN's own evidence for this criterion).
    Write the VERIFICATION.md using frontmatter `plan: "21-01"`, `phase: 21`, `status: verified`, `verified_at: "2026-06-16"`. Include a Phase Goal section (copy Phase 21 goal from ROADMAP/PLAN), a Truth Checks table (one row per criterion with Status + Evidence = actual command output), and a Requirements Satisfied section listing TYPE-02 and TYPE-03.
    In REQUIREMENTS.md: change TYPE-02 checkbox `[ ]` → `[x]` (line ~13) and TYPE-03 checkbox `[ ]` → `[x]` (line ~14). In the Traceability table, change `| TYPE-02 | Phase 21 | Pending |` → `| TYPE-02 | Phase 21 | Complete |` and `| TYPE-03 | Phase 21 | Pending |` → `| TYPE-03 | Phase 21 | Complete |` (D-07). Do NOT touch QUAL-01's row here — QUAL-01 maps to Phase 20 in the table and was already closed there; this phase only closed the residual assignment sites.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && test -f .planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md && grep -q "TYPE-02" .planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md && grep -q "TYPE-03" .planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md && grep -q "\- \[x\] \*\*TYPE-02\*\*" .planning/REQUIREMENTS.md && grep -q "\- \[x\] \*\*TYPE-03\*\*" .planning/REQUIREMENTS.md && grep -q "| TYPE-02 | Phase 21 | Complete |" .planning/REQUIREMENTS.md && grep -q "| TYPE-03 | Phase 21 | Complete |" .planning/REQUIREMENTS.md && echo "VERIFICATION_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/21-concrete-types-plugin-boundary/21-01-VERIFICATION.md` exists with frontmatter `phase: 21` and a Truth Checks table containing 4 rows (one per Phase 21 success criterion) with real command-output evidence
    - REQUIREMENTS.md TYPE-02 and TYPE-03 checklist entries are `[x]`
    - REQUIREMENTS.md Traceability table shows TYPE-02 and TYPE-03 as `Complete`
    - The verify command prints `VERIFICATION_OK`
  </acceptance_criteria>
  <done>Phase 21 VERIFICATION.md written with live evidence for all 4 criteria; REQUIREMENTS.md marks TYPE-02/TYPE-03 complete in checklist and Traceability table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config → device construction | User-authored YAML is parsed into Pydantic models; invalid fields must raise validation errors, not runtime AttributeErrors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | DeviceAction JSON serialization (state.json) | accept | StrEnum serializes to `.value`; JSON output byte-identical to prior `str`/`Literal` — no state-file format change, no migration needed |
| T-24-02 | Information disclosure | none | accept | Internal refactor only; no new I/O, no secrets, no network surface |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all changes are edits to existing typed source; no new dependencies |
</threat_model>

<verification>
- `make test` passes with no new regressions (compare against the 309-green baseline from Phase 23)
- `grep -rn "list\[Any\]" packages/*/src/*/device.py` returns zero hits (all plugin preset fields typed)
- `grep -rn 'device_type="' packages/rig/src/rig/engine/ packages/rig/src/rig/cli/` returns zero hits
- Phase 21 VERIFICATION.md exists and records all 4 criteria as PASS with live evidence
- REQUIREMENTS.md TYPE-02 and TYPE-03 marked complete (checklist + Traceability)
</verification>

<success_criteria>
1. MC6Device.presets is typed `list[Preset]`; no `list[Any]` remains in any plugin device class
2. DeviceAction.device_type is `DeviceType`; DeviceAction.status is `ActionStatus` (new StrEnum)
3. All 4 DeviceAction construction sites and the plan.py status consumers use enum members — zero raw device-type strings in engine/cli construction
4. Phase 21 VERIFICATION.md exists with live evidence for all 4 Phase 21 success criteria
5. REQUIREMENTS.md marks TYPE-02 and TYPE-03 complete
6. `make test` passes with no new regressions
</success_criteria>

<output>
Create `.planning/phases/24-close-v1-4-gaps-phase-21-verification-mc6-preset-typing-qual/24-01-SUMMARY.md` when done
</output>
