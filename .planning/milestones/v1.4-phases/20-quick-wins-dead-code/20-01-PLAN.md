---
id: "20-01"
phase: 20
title: "Quick Wins & Dead Code"
type: execute
goal: "The codebase has no dead stubs, no broken test infrastructure, and a recorded decision on the Enums-vs-Literals question"
requirements: ["TYPE-04", "TEST-01", "QUAL-01", "QUAL-02"]
wave: 1
depends_on: []
autonomous: true
files_modified:
  - packages/rig/src/rig/engine/plugin.py
  - packages/rig-analog/src/rig_analog/device.py
  - packages/rig-hx/src/rig_hx/device.py
  - packages/rig-chasebliss/src/rig_chasebliss/device.py
  - packages/rig-morningstar/src/rig_morningstar/device.py
  - packages/rig/tests/test_devices.py
  - packages/rig/tests/test_plugin.py
  - tests/
  - Makefile
  - packages/rig/src/rig/engine/plan/compute.py
  - packages/rig/src/rig/cli/commands/plan.py
  - packages/rig/src/rig/models/device.py

must_haves:
  truths:
    - "A grep for the dead Device-Protocol stubs (def plan(self, ctx: / def diff(self, ctx:) in non-test src returns zero hits"
    - "make test completes pytest collection with no import errors"
    - "No raw DeviceType string comparisons (== \"analog\" / \"digital\" / \"controller\" / \"modeler\") remain in src/"
    - "The Enums-vs-Literals TODO is replaced with an inline explanation"
    - "uv run pytest packages/ -q still passes everything except the 3 pre-existing stdin-capture failures (no new failures)"
  artifacts:
    - path: "packages/rig/src/rig/engine/plugin.py"
      provides: "Device Protocol without plan/diff stubs"
    - path: "Makefile"
      provides: "test target that runs pytest without stale tests/ path"
    - path: "packages/rig/src/rig/models/device.py"
      provides: "DeviceType with documented Enums-vs-Literals rationale"
  key_links:
    - from: "packages/rig-*/src/rig_*/device.py"
      to: "rig.engine.plugin"
      via: "import line without PluginContext"
      pattern: "from rig.engine.plugin import DeviceApplyContext, SetupContext, SetupResult"
---

<objective>
Phase 20 removes accumulated cruft surfaced by the v1.4 architecture audit: dead `plan()`/`diff()` Device-Protocol stubs (TYPE-04), a stale root `tests/` directory that breaks `make test` collection (TEST-01), three raw-string `DeviceType` comparisons that bypass the enum (QUAL-01), and a stale TODO comment about Enums-vs-Literals (QUAL-02).

Purpose: All four are independent, zero-cross-phase-risk cleanups. They reduce dead surface area and unblock a working `make test` so later v1.4 phases run on a clean baseline.

Output: Cleaned Protocol + 4 plugin device modules, deleted root `tests/`, fixed Makefile, enum-based comparisons, and a documented design decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-quick-wins-dead-code/RESEARCH.md

# Engineering standards (project)
# - Commit after each logical unit. Conventional commits: type(scope): description (scope REQUIRED).
# - Run `uv run pytest packages/ -q` after each change set.
# - No Co-Authored-By trailer.
</context>

<baseline>
Before starting, know the current state:
- `make test` → BROKEN (9 collection errors from the stale root `tests/` dir).
- `uv run pytest packages/ -q` → **3 failed, 302 passed**. The 3 failures are pre-existing stdin-capture issues in `test_appliers` and `test_apply`. Do NOT regress them and do NOT try to fix them — they are out of scope. "Success" means **still exactly 3 failures, same tests**, after this phase's test deletions are accounted for.
- Each of the 4 tasks below is fully independent and committed individually.
</baseline>

<tasks>

<task type="auto">
  <name>Task 1 (TYPE-04): Remove dead plan()/diff() stubs from Protocol, plugins, and tests</name>
  <files>packages/rig/src/rig/engine/plugin.py, packages/rig-analog/src/rig_analog/device.py, packages/rig-hx/src/rig_hx/device.py, packages/rig-chasebliss/src/rig_chasebliss/device.py, packages/rig-morningstar/src/rig_morningstar/device.py, packages/rig/tests/test_devices.py, packages/rig/tests/test_plugin.py</files>
  <action>
Delete the dead `plan()`/`diff()` Device-Protocol stubs everywhere they appear. They were never called — the plan command routes through `rig.engine.plan.compute.compute_plan()`. Make ALL of the following edits:

PROTOCOL — `packages/rig/src/rig/engine/plugin.py`:
1. Protocol stubs (~lines 142-144): DELETE the two lines:
   `    def plan(self, ctx: PluginContext) -> object: ...`
   `    def diff(self, ctx: PluginContext) -> object: ...`
2. Docstring example (~lines 109-113): DELETE the two example method blocks (`def plan(self, ctx: PluginContext) -> object:` / `raise NotImplementedError` and the matching `def diff...` block) from the `Example::` docstring.
3. Docstring example import line (~line 98): in the `from rig.engine.plugin import ( ... )` example tuple, REMOVE `PluginContext` so the example no longer references it (leave `Device, DeviceApplyContext, DeviceApplyResult, SetupContext, SetupResult`).
4. Module-level docstring "Required methods" bullet (~lines 19-24): DELETE the bullet that begins `- **plan** / **diff** — currently raise ``NotImplementedError``;` and its continuation lines, so the list starts at the `- **setup(ctx)**` bullet.
   IMPORTANT: Do NOT delete the `class PluginContext:` definition (~line 49) or the `PluginContext` import/dataclass — it is still a live type used by `test_plugin.py`'s dedicated PluginContext tests. Only the plan/diff *stub usages* go.

PLUGIN IMPLEMENTATIONS — in each of the 4 device files, DELETE the identical 4-line stub block (`def plan(self, ctx: PluginContext) -> object:` / `raise NotImplementedError` / `def diff(self, ctx: PluginContext) -> object:` / `raise NotImplementedError`):
   - `packages/rig-analog/src/rig_analog/device.py` (~lines 42-46)
   - `packages/rig-hx/src/rig_hx/device.py` (~lines 58-62)
   - `packages/rig-chasebliss/src/rig_chasebliss/device.py` (~lines 202-206)
   - `packages/rig-morningstar/src/rig_morningstar/device.py` (~lines 39-43)

PLUGINCONTEXT IMPORT — `PluginContext` becomes unused in all 4 plugin device files after the stubs are gone. In EACH of the 4 files, change the import:
   FROM: `from rig.engine.plugin import DeviceApplyContext, PluginContext, SetupContext, SetupResult`
   TO:   `from rig.engine.plugin import DeviceApplyContext, SetupContext, SetupResult`
   (rig-hx imports `DeviceType` separately on line 14 — leave that untouched.)

TEST CLEANUP — `packages/rig/tests/test_devices.py`:
   - DELETE these 8 test functions entirely (each ~9 lines, locations approximate): `test_analog_device_plan_raises_not_implemented` (~116), `test_analog_device_diff_raises_not_implemented` (~125), `test_midi_device_plan_raises_not_implemented` (~187), `test_midi_device_diff_raises_not_implemented` (~196), `test_chase_bliss_device_plan_raises_not_implemented` (~240), `test_chase_bliss_device_diff_raises_not_implemented` (~249), `test_mc6_device_plan_raises_not_implemented` (~293), `test_mc6_device_diff_raises_not_implemented` (~302).
   - UPDATE the structural-check tuple (~line 389):
     FROM: `for attr in ("id", "name", "config", "plan", "diff", "apply"):`
     TO:   `for attr in ("id", "name", "config", "apply"):`

TEST CLEANUP — `packages/rig/tests/test_plugin.py`:
   - `test_device_protocol_satisfied_by_class_with_all_members` (~line 55): in the `GoodDevice` inner class remove the `def plan(self, ctx: PluginContext) -> object:` and `def diff(self, ctx: PluginContext) -> object:` method bodies (~lines 71-75), and remove the two assertions `assert hasattr(device, "plan")` / `assert hasattr(device, "diff")` (~lines 84-85).
   - `test_device_protocol_missing_property_detected_via_hasattr` (~line 89): remove the two assertions `assert not hasattr(device, "plan")` / `assert not hasattr(device, "diff")` (~lines 105-106).
   - `test_device_protocol_has_distinct_context_types` (~line 110): DELETE the entire test function. It reads `Device.plan.__annotations__`, which no longer exists once the stub is removed — leaving it would raise `AttributeError`.
   - `test_discovery_placeholder_implements_device_protocol` (~line 365): remove the two assertions `assert hasattr(device, "plan")` / `assert hasattr(device, "diff")` (~lines 374-375).
   - Do NOT remove the top-level `from rig.engine.plugin import PluginContext` import (~line 5) — the dedicated PluginContext tests (~lines 14-39) still use it.

After edits, the only remaining `def plan(` / `def diff(` in non-test src should be the legitimate CLI command functions in `cli/commands/plan.py` and `cli/commands/diff.py` (signature `def plan(` / `def diff(` with no `self, ctx:`). Those are NOT stubs — leave them.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! grep -rn "def plan(self, ctx:" packages/*/src/ && ! grep -rn "def diff(self, ctx:" packages/*/src/ && ! grep -rn "PluginContext" packages/rig-analog/src/ packages/rig-hx/src/ packages/rig-chasebliss/src/ packages/rig-morningstar/src/ && uv run pytest packages/ -q 2>&1 | tail -3</automated>
  </verify>
  <done>Zero `def plan(self, ctx:` / `def diff(self, ctx:` hits in `packages/*/src/`; no `PluginContext` references in the 4 plugin src trees; `uv run pytest packages/ -q` shows the same 3 pre-existing failures and no new ones (deleted-test count reflected in the total). Commit: `refactor(plugin): remove dead plan/diff Device Protocol stubs`</done>
</task>

<task type="auto">
  <name>Task 2 (TEST-01): Delete stale root tests/ and fix Makefile test target</name>
  <files>tests/, Makefile</files>
  <action>
The root `tests/` directory is a stale leftover from the pre-monorepo layout. Its modules import paths that no longer exist, producing 9 pytest collection errors and breaking `make test`. The live tests are under `packages/*/tests/`.

Make BOTH edits in a SINGLE commit (deleting the dir without fixing the Makefile, or vice-versa, leaves `make test` broken):

1. DELETE the entire root directory `/Users/derekgliwa/dev/rig-cli/tests/` (e.g. `git rm -r tests/`). This includes `__init__.py`, `fakes.py`, `test_appliers.py`, `test_apply.py`, `test_base_helpers.py`, `test_catalog.py`, `test_cli.py`, `test_cli_plan.py`, `test_devices.py`, `test_diff.py`, `test_graph.py`, `test_loader.py`, `test_logging.py`, `test_mc6_generator.py`, `test_mc6_sysex.py`, `test_midi_adapter.py`, `test_models.py`, `test_plan.py`, `test_plugin.py`, and `__pycache__/`.
   NOTE: these are the STALE root copies — the canonical versions live in `packages/*/tests/`. Do not touch anything under `packages/`.

2. UPDATE `Makefile`, the `test:` target:
   FROM: `	uv run pytest tests/ -v`
   TO:   `	uv run pytest -v`
   (pytest will then discover tests via the configured testpaths / package layout. Leave the `## Run all tests` help comment and the tab indentation intact.)
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && test ! -d tests && grep -q "uv run pytest -v" Makefile && make test 2>&1 | grep -iE "error during collection|ImportError" | head -5; echo "exit-collection-check-done"</automated>
  </verify>
  <done>Root `tests/` directory no longer exists; `Makefile` `test:` target runs `uv run pytest -v`; `make test` collects with no "error during collection" / ImportError lines (only the 3 pre-existing stdin-capture test failures remain). Commit: `chore(test): delete stale root tests dir and fix make test target`</done>
</task>

<task type="auto">
  <name>Task 3 (QUAL-01): Replace raw DeviceType string comparisons with enum members</name>
  <files>packages/rig/src/rig/engine/plan/compute.py, packages/rig/src/rig/cli/commands/plan.py, packages/rig-hx/src/rig_hx/device.py</files>
  <action>
There are exactly 3 raw-string `DeviceType` comparisons that bypass the `DeviceType` StrEnum. Replace each with the enum member. Make these edits:

1. `packages/rig/src/rig/engine/plan/compute.py` line ~88:
   FROM: `            if pedal.type.value == "analog":`
   TO:   `            if pedal.type == DeviceType.ANALOG:`
   (Note: drop the `.value` — `pedal.type` is already a `DeviceType`.)
   ADD import (not currently present in this file): `from rig.models.device import DeviceType`
   Place it with the other `from rig.models...` imports (the file already imports `from rig.models.graph import DeviceGraph` and `from rig.models.rig import Rig` ~lines 8-9; add it alongside, keeping isort/import ordering: `rig.models.device` sorts before `rig.models.graph`).

2. `packages/rig/src/rig/cli/commands/plan.py` line ~97:
   FROM: `            if action.device_type == "analog":`
   TO:   `            if action.device_type == DeviceType.ANALOG:`
   ADD import (not currently present): `from rig.models.device import DeviceType`
   Place it with the other `from rig...` imports (file imports `from rig.config.errors import ConfigError`, `from rig.config.loader import load_rig`, `from rig.engine.plan import compute_plan` ~lines 17-19; add `from rig.models.device import DeviceType` in correct isort order — `rig.models.device` sorts after `rig.engine.*` and after `rig.log_setup`).
   If `action.device_type` is a plain `str` rather than a `DeviceType`, the comparison still holds because `DeviceType` is a `StrEnum` (`DeviceType.ANALOG == "analog"` is `True`). Use the enum member regardless for consistency.

3. `packages/rig-hx/src/rig_hx/device.py` line ~40:
   FROM: `        if data.get("type") == "modeler":`
   TO:   `        if data.get("type") == DeviceType.MODELER:`
   NO import change — `DeviceType` is already imported on line 14 (`from rig.models.device import DeviceType`).
   (`data.get("type")` is the raw YAML string; `DeviceType.MODELER == "modeler"` holds because `DeviceType` is a `StrEnum`.)

Do NOT change `== "digital"` / `== "controller"` comparisons elsewhere — research confirms only these 3 occurrences exist for the four target literals.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! grep -rnE '== "(analog|digital|controller|modeler)"' packages/*/src/ && uv run pytest packages/rig/tests/test_plan.py packages/rig/tests/test_cli_plan.py packages/rig-hx/tests -q 2>&1 | tail -3</automated>
  </verify>
  <done>Grep for `== "analog"`, `== "digital"`, `== "controller"`, `== "modeler"` in `packages/*/src/` returns zero hits; the three files compile and import; affected plan/HX tests pass. Commit: `refactor(engine): use DeviceType enum for device-type comparisons`</done>
</task>

<task type="auto">
  <name>Task 4 (QUAL-02): Replace Enums-vs-Literals TODO with inline rationale</name>
  <files>packages/rig/src/rig/models/device.py</files>
  <action>
`packages/rig/src/rig/models/device.py` line ~11 has a stale TODO immediately above `class DeviceType(StrEnum):`. The Enums-vs-Literals question is intentional (StrEnum for runtime comparisons, Literal for Pydantic discriminated-union dispatch), so replace the TODO with a documented decision rather than leaving an open question.

REPLACE the single line:
`# TODO: we inconsistently use Enums and Literals. Pick one (whichever is the better python convention)`

WITH these four comment lines (keep them directly above `class DeviceType(StrEnum):`):
`# DeviceType StrEnum is for runtime comparisons (engine, CLI, plan output).`
`# Config submodels use Literal type fields (e.g. Literal["chase_bliss"]) — Pydantic`
`# requires Literal for discriminated union dispatch. The two are orthogonal: StrEnum`
`# describes device kind; Literal identifies config schema. Both must coexist.`

Leave the `class DeviceType(StrEnum):` line and its members unchanged.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! grep -n "TODO: we inconsistently use Enums" packages/rig/src/rig/models/device.py && grep -q "Both must coexist" packages/rig/src/rig/models/device.py && uv run python -c "import rig.models.device"</automated>
  </verify>
  <done>The `TODO: we inconsistently use Enums` line is gone; the four-line rationale ending in "Both must coexist." is present above `class DeviceType`; module imports cleanly. Commit: `docs(models): document Enums-vs-Literals decision on DeviceType`</done>
</task>

<task type="auto">
  <name>Task 5 (Verification): Confirm all success criteria and clean test baseline</name>
  <files>(none — verification only)</files>
  <action>
Run the full success-criteria verification after Tasks 1-4 are committed. Confirm each of the four phase success criteria:

1. TYPE-04 — no dead stubs: `grep -rn "def plan(self, ctx:" packages/*/src/` and the `def diff(self, ctx:` variant both return zero hits. (The CLI `def plan(` / `def diff(` command functions are expected and fine.)
2. TEST-01 — collection clean: `make test` produces no "error during collection" / ImportError lines; the only failures are the 3 pre-existing stdin-capture tests.
3. QUAL-01 — no raw comparisons: `grep -rnE '== "(analog|digital|controller|modeler)"' packages/*/src/` returns zero hits.
4. QUAL-02 — TODO replaced: `grep -n "TODO: we inconsistently use Enums" packages/rig/src/rig/models/device.py` returns zero hits.

Then confirm no regression in the test suite: `uv run pytest packages/ -q` shows the same 3 pre-existing failures (test_appliers / test_apply stdin-capture) and NO new failures. The passing count will be lower than the original 302 because Task 1 deleted 9 now-obsolete tests (8 plan/diff-raises tests in test_devices.py + the distinct_context_types test in test_plugin.py) — that decrease is expected, not a regression.

If any criterion fails, identify which task's edit was incomplete and report it. Do NOT attempt to fix the 3 pre-existing stdin-capture failures — they are out of scope.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! grep -rn "def plan(self, ctx:" packages/*/src/ && ! grep -rn "def diff(self, ctx:" packages/*/src/ && ! grep -rnE '== "(analog|digital|controller|modeler)"' packages/*/src/ && ! grep -n "TODO: we inconsistently use Enums" packages/rig/src/rig/models/device.py && test ! -d tests && uv run pytest packages/ -q 2>&1 | tail -4</automated>
  </verify>
  <done>All four success-criteria greps pass (zero hits each), root `tests/` is gone, and `uv run pytest packages/ -q` reports exactly the 3 pre-existing failures with zero new failures. No commit needed (verification only); if anything changed during verification, commit with an appropriate `fix(...)` message.</done>
</task>

</tasks>

<verification>
Phase-level checks (all must pass):
- `grep -rn "def plan(self, ctx:" packages/*/src/` → 0 hits; same for `def diff(self, ctx:`.
- `grep -rn "PluginContext" packages/rig-analog/src packages/rig-hx/src packages/rig-chasebliss/src packages/rig-morningstar/src` → 0 hits.
- `test ! -d tests` → true (root tests/ deleted); `Makefile` test target = `uv run pytest -v`.
- `make test` → no collection/import errors.
- `grep -rnE '== "(analog|digital|controller|modeler)"' packages/*/src/` → 0 hits.
- `grep -n "TODO: we inconsistently use Enums" packages/rig/src/rig/models/device.py` → 0 hits; "Both must coexist." present.
- `uv run pytest packages/ -q` → same 3 pre-existing stdin-capture failures only; no new failures.
</verification>

<success_criteria>
1. `plan()`/`diff()` Device-Protocol stubs removed from the Protocol and all 4 plugin implementations; non-test src grep for the stub signatures returns zero hits.
2. `make test` completes collection with no import errors; the stale root `tests/` directory is gone.
3. Grep for `== "analog"`, `== "digital"`, `== "controller"`, `== "modeler"` in `packages/*/src/` returns zero hits; comparisons use `DeviceType` enum members.
4. The Enums-vs-Literals TODO is replaced with an inline explanation.
5. No new test failures introduced (the 3 pre-existing stdin-capture failures are unchanged and out of scope).
</success_criteria>

<output>
Create `.planning/phases/20-quick-wins-dead-code/20-01-SUMMARY.md` when done.
</output>
