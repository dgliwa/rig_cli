# Milestone v1.2: Cleaner Core

**Status:** ✅ SHIPPED 2026-06-08
**Phases:** 9–13
**Total Plans:** 8

## Overview

Collapse the multi-file config repo into a single flat `rig.yaml`, strip all plugin-specific code from core, and remove backwards-compat shims introduced in v1.0/v1.1. Included Phase 13 (originally scoped as v1.3) since the REQUIREMENTS.md covered all these items together and phase 13 completed the same day.

## Phases

### Phase 9: Core Model Cleanup

**Goal**: Core models contain no plugin-specific types, no compat shims, and no deprecated fields — the domain is expressed purely through `type`-keyed device identity and the new schema primitives
**Depends on**: Phase 8 (v1.1 dead code eliminated)
**Requirements**: MODEL-01, MODEL-02, MODEL-03, MODEL-04, SCHEMA-03, SCHEMA-06
**Plans**: 4 plans

Plans:
- [x] 09-01: Remove Scene mc6 fields; make Rig.scenes a direct Pydantic field; remove Rig compat properties
- [x] 09-02: Simplify signal chain from list[SignalChainPosition] to list[str]; delete signal_chain.py
- [x] 09-03: Move plugin config types (Control, ControlType, ChaseBlissConfig) to rig-chasebliss; remove CBA plan-time detection; make Device.config: Any
- [x] 09-04: Remove manufacturer/model from Device (SCHEMA-06)

**Success Criteria Achieved:**
1. `device.py` imports contain no `ManualConfig`, `MidiConfig`, `ChaseBlissConfig`, or `ControllerConfig` ✓
2. `Control` and `ControlType` classes absent from `packages/rig/src/rig/models/` ✓
3. `Rig` model has no `pedals`, `digital_presets`, `hx_presets`, `analog_presets`, `mc6`, or `_controller_device` attributes ✓
4. `Scene` model has no `mc6_bank` or `mc6_switch` fields ✓
5. `SignalChainPosition` class absent from core models ✓

---

### Phase 10: Schema & Loader Rewrite

**Goal**: Users can run `rig validate` against a single `rig.yaml` where device list order defines the signal chain, controller devices compose other devices by ID, and scenes live inside the controller
**Depends on**: Phase 9
**Requirements**: SCHEMA-01, SCHEMA-02, SCHEMA-04, SCHEMA-05, LOADER-01, LOADER-02
**Plans**: 1 plan

Plans:
- [x] 10-01: Rewrite loader for single-file rig.yaml; update fixture and tests

**Success Criteria Achieved:**
1. `rig validate path/to/rig.yaml` succeeds with single flat device list ✓
2. Device list order treated as signal chain — `signal-chain.yaml` not read or required ✓
3. Controller device `composes:` validated against device IDs ✓
4. Scenes inside controller with `bank`, `switch`, `presets:` loaded via `Rig.scenes` ✓
5. `config.type` is the entry point lookup key — unknown types produce a clear error ✓

---

### Phase 11: Dead Code & Compat Removal

**Goal**: The codebase contains no `TODO: 1.2` markers, no multi-file config parsing paths, and no code retained solely for backwards compatibility with the pre-v1.2 config layout
**Depends on**: Phase 10
**Requirements**: CLEANUP-01, CLEANUP-02
**Plans**: 1 plan

Plans:
- [x] 11-01: Remove all TODO:1.2 markers; remove multi-file loader paths; move preset types to plugins

**Success Criteria Achieved (from UAT):**
1. `grep -rn "TODO.*1\.2" packages/rig/src/` returns zero results ✓
2. `load_rig()` has no code paths reading `signal-chain.yaml`, `scenes/*.yaml`, `pedals/*.yaml`, or `mc6.yaml` ✓
3. All tests pass after removal ✓

---

### Phase 12: Clean Up Deferred Items

**Goal**: Remove vestigial `generate_mc6` command and resolve the two deferred items from Phase 10
**Depends on**: Phase 11
**Requirements**: DEFER-01, DEFER-02
**Plans**: 1 plan

Plans:
- [x] 12-01: Delete generate_mc6 command and morningstar generator; remove composes validation

**Success Criteria Achieved:**
1. `rig generate mc6` CLI command removed ✓
2. `rig_morningstar.generator` module deleted ✓
3. `composes` validation removed from loader ✓
4. 262 tests pass ✓

---

### Phase 13: Resolve Remaining TODO:1.2 Markers (INSERTED)

**Goal**: Address the 3 remaining `TODO: 1.2` architectural notes — move scenes off the flat `Rig.scenes` model onto controllers where they belong, and unify HX preset-number lookup
**Depends on**: Phase 12
**Requirements**: CLEANUP-01, DESIGN-01, DESIGN-02
**Plans**: 1 plan

Plans:
- [x] 13-01: Convert Rig.scenes to @property derived from controller config; remove is_hx branch from compute.py

**Success Criteria Achieved (from UAT):**
1. No TODO:1.2 markers in source ✓
2. `Rig.scenes` is a `@property`, not a stored field; loader does not pass `scenes=` to Rig constructor ✓
3. No `is_hx` branch in compute.py; all preset_number lookups go through `_get_preset_number` ✓
4. All tests pass ✓

---

## Milestone Summary

**Key Decisions:**
- `Device.config: Any` (not discriminated union) — plugin device models handle both dict and model configs via isinstance checks
- Device list order = signal chain — no separate `signal-chain.yaml` or positional fields
- `config.type` is the entry point key — separate from `device.type` (DeviceType enum)
- Scenes extracted from controller config — `Rig.scenes` is a computed property over controller devices
- `_get_preset_number` unified for all preset types — eliminates HX-specific branching in compute.py

**Issues Resolved:**
- All `TODO: 1.2` markers cleared across the codebase
- Multi-file config layout support fully removed
- All plugin config types evicted from core

**Issues Deferred:**
- `Device.plan()` / `Device.diff()` stubs still raise NotImplementedError — dead code from Phase 4; tracked in PROJECT.md Key Decisions

**Technical Debt Incurred:**
- Phases 11 and 13 executed without SUMMARY.md artifacts (GSD workflow gap — same pattern as v1.1 Phases 7/8)

---

*For current project status, see .planning/ROADMAP.md*
