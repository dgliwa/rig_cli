---
phase: 02-engine-i-o-decoupling
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/rig/engine/apply.py
  - src/rig/cli/commands/apply.py
  - tests/test_appliers.py
  - tests/test_apply.py
autonomous: true
requirements:
  - DEC-05
  - DEC-06
  - DEC-07

must_haves:
  truths:
    - "apply_plan accepts required state_writer: StateWriter and midi_connection_io: MidiConnectionIO parameters"
    - "apply_plan no longer calls read_state, write_state, or prompt_midi_connect directly"
    - "Scene state is only written when at least one device action returned status == confirmed"
    - "cli/commands/apply.py constructs production adapters and passes them to apply_plan"
    - "test_appliers.py has zero patch(builtins.input) calls"
    - "test_apply.py has zero patch(builtins.input) calls"
    - "All tests pass without MIDI hardware"
  artifacts:
    - path: "src/rig/engine/apply.py"
      provides: "apply_plan with state_writer and midi_connection_io required params"
      contains: "state_writer: StateWriter"
    - path: "src/rig/cli/commands/apply.py"
      provides: "CLI wiring of production adapters"
      contains: "RichConfirmationIO"
    - path: "tests/test_appliers.py"
      provides: "Applier tests using fakes"
    - path: "tests/test_apply.py"
      provides: "apply_plan integration tests using fakes"
  key_links:
    - from: "src/rig/cli/commands/apply.py"
      to: "src/rig/engine/apply.py:apply_plan"
      via: "state_writer=FileStateWriter(), midi_connection_io=InteractiveMidiConnectionIO()"
      pattern: "FileStateWriter|InteractiveMidiConnectionIO"
    - from: "src/rig/engine/apply.py"
      to: "state_writer.write"
      via: "replaces write_state(config_path, state)"
      pattern: "state_writer\\.write"
    - from: "src/rig/engine/apply.py"
      to: "midi_connection_io.prompt_connect"
      via: "replaces prompt_midi_connect call"
      pattern: "midi_connection_io\\.prompt_connect"
---

<objective>
Wire the Protocols from plan 02-01 into apply_plan and the CLI, fix the DEC-06 scene-write bug, then rewrite all builtins.input-patching tests to use the fakes.

Purpose: Complete the decoupling. After this plan, the entire apply engine can be exercised in tests by passing fakes — no hardware, no monkey-patching. Production behaviour is identical because the production adapters (FileStateWriter, InteractiveMidiConnectionIO, RichConfirmationIO) delegate to the same functions that were previously called inline.

Output: apply.py with two new required params and the scene-write bug fixed; cli/commands/apply.py wiring production adapters; test_appliers.py and test_apply.py fully converted to fakes.
</objective>

<execution_context>
@/Users/derekgliwa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/derekgliwa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/derekgliwa/dev/rig-cli/.planning/ROADMAP.md
@/Users/derekgliwa/dev/rig-cli/.planning/phases/02-engine-i-o-decoupling/02-CONTEXT.md
@/Users/derekgliwa/dev/rig-cli/.planning/phases/02-engine-i-o-decoupling/02-01-SUMMARY.md
@/Users/derekgliwa/dev/rig-cli/src/rig/engine/apply.py
@/Users/derekgliwa/dev/rig-cli/src/rig/cli/commands/apply.py
@/Users/derekgliwa/dev/rig-cli/src/rig/engine/ports.py
@/Users/derekgliwa/dev/rig-cli/tests/test_appliers.py
@/Users/derekgliwa/dev/rig-cli/tests/test_apply.py
@/Users/derekgliwa/dev/rig-cli/tests/fakes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update apply_plan — add required params, route through adapters, fix DEC-06 scene-write bug (per D-05, DEC-06)</name>
  <files>src/rig/engine/apply.py</files>
  <action>
Imports to add in apply.py:
- from rig.engine.ports import ConfirmationIO, MidiConnectionIO, StateWriter

Imports to remove from apply.py:
- read_state and write_state from rig.engine.state (they are no longer called directly)
- prompt_midi_connect from rig.interaction.midi (replaced by midi_connection_io.prompt_connect)

Note: collect_midi_devices is still imported from rig.interaction.midi — keep that import.

Signature change (per D-05 — required params, no defaults; confirmation_io optional for testability):
Add two required parameters and one optional parameter to apply_plan. The updated signature is:

  def apply_plan(
      plan: Plan,
      state_writer: StateWriter,
      midi_connection_io: MidiConnectionIO,
      confirmation_io: ConfirmationIO | None = None,
      rig: Rig | None = None,
      config_path: str | None = None,
      dry_run: bool = False,
      scene: str | None = None,
      midi: MidiManager | None = None,
  ) -> ApplyResult:

`state_writer` and `midi_connection_io` are required (no defaults) per D-05. `confirmation_io` is optional (defaults to None) so tests can inject a fake without modifying the CLI — the CLI does not pass it, production code gets RichConfirmationIO via `confirmation_io or RichConfirmationIO()`.

Body changes in apply_plan:

1. State read (lines 61-63): Replace `state = read_state(config_path)` with `state = state_writer.read(config_path)`. Keep the `if config_path:` guard — if no config_path, state stays as the empty RigState() already assigned.

2. ApplyContext construction (lines 65-71): Add `confirmation_io=confirmation_io or RichConfirmationIO()` to the ApplyContext constructor call. Import ConfirmationIO and RichConfirmationIO from rig.engine.ports (add to the import line above).

3. MIDI connect prompt (line 106): Replace `res, port_name = prompt_midi_connect(device_id, ch, midi, cached_port)` with `res, port_name = midi_connection_io.prompt_connect(device_id, ch, midi, cached_port)`.

4. Scene-write bug fix (line 171, per DEC-06): The current code unconditionally writes `state.scenes[sp.scene_name] = {}` and sets `state_modified = True` regardless of whether any device action returned "confirmed". Fix: replace those two lines with a conditional block:

   if any(r.status == "confirmed" for r in device_results):
       state.scenes[sp.scene_name] = {}
       state_modified = True

5. State write (lines 191-193): Replace `write_state(config_path, state)` with `state_writer.write(config_path, state)`. Keep the existing `if config_path and not dry_run and state_modified:` guard unchanged.

No other logic changes. All console.print calls, CBA setup phase, MC6 programming phase — untouched.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run ruff check src/rig/engine/apply.py && uv run python -c "import inspect; from rig.engine.apply import apply_plan; sig = inspect.signature(apply_plan); params = list(sig.parameters); assert 'state_writer' in params and 'midi_connection_io' in params, 'params missing'; print('apply_plan signature OK:', params)"</automated>
  </verify>
  <done>apply_plan has state_writer and midi_connection_io as required params. read_state and write_state are no longer imported directly. The scene-write block is guarded by any(r.status == "confirmed"). Ruff exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire production adapters in cli/commands/apply.py (per D-05)</name>
  <files>src/rig/cli/commands/apply.py</files>
  <action>
This is the single production wiring point per D-03. Changes:

1. Add import: from rig.engine.ports import FileStateWriter, InteractiveMidiConnectionIO, RichConfirmationIO

2. In the apply command body, construct adapters before the apply_plan call:
   state_writer = FileStateWriter()
   midi_connection_io = InteractiveMidiConnectionIO()

   The confirmation_io adapter (RichConfirmationIO) is now constructed inside apply_plan itself when building ApplyContext (done in Task 1 of this plan). So only state_writer and midi_connection_io are explicitly constructed here.

3. Update the apply_plan call to pass the new required params:
   apply_plan(
       result,
       state_writer=state_writer,
       midi_connection_io=midi_connection_io,
       rig=rig,
       config_path=config_path,
       dry_run=dry_run,
       scene=scene,
       midi=midi,
   )

No other changes to the apply command. The MidiManager instantiation and midi.disconnect_all() call remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run ruff check src/rig/cli/commands/apply.py && uv run python -c "from rig.cli.commands.apply import apply; print('cli apply command imports OK')"</automated>
  </verify>
  <done>cli/commands/apply.py imports FileStateWriter and InteractiveMidiConnectionIO, constructs them, and passes them as keyword args to apply_plan. Ruff exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite all builtins.input-patching tests to use fakes; make full test suite pass (per D-06, D-07)</name>
  <files>tests/test_appliers.py, tests/test_apply.py</files>
  <action>
CRITICAL: After this task, `grep -rn 'patch("builtins.input"' tests/` must return zero results.

tests/test_appliers.py rewrites:

Update _make_ctx to accept and pass confirmation_io:
  def _make_ctx(dry_run=False, connected=None, confirmation_io=None):
      from tests.fakes import InMemoryPromptAdapter
      midi = MagicMock()
      return ApplyContext(
          dry_run=dry_run,
          confirmation_io=confirmation_io or InMemoryPromptAdapter(default="skip"),
          midi=midi,
          connected_devices=connected or set(),
          state=RigState(),
      )

Note: default="skip" means any test that does not configure responses will get "skip" back — consistent with the previous "no action" state for tests that only check dry_run paths.

Add import at top: from tests.fakes import InMemoryPromptAdapter

For each test that uses patch("builtins.input", return_value="c"): replace with an InMemoryPromptAdapter(default="confirm") passed via _make_ctx(confirmation_io=...).

For each test that uses patch("builtins.input", return_value="s"): replace with InMemoryPromptAdapter(default="skip").

For each test that uses patch("builtins.input", return_value="q"): replace with InMemoryPromptAdapter(default="quit").

For each test that uses patch("builtins.input", side_effect=["r", "c"]): replace with InMemoryPromptAdapter(side_effect=["retry", "confirm"]).

For TestChaseBlissApplierSetup.test_establish_channel_confirm_sets_channel_established which uses side_effect=["c", "c"]: replace with InMemoryPromptAdapter(side_effect=["confirm", "confirm"]).

Remove the `from unittest.mock import MagicMock, patch` line and replace with `from unittest.mock import MagicMock` (patch is no longer needed).

tests/test_apply.py rewrites:

Add imports:
  from tests.fakes import InMemoryMidiConnectionIO, InMemoryPromptAdapter, InMemoryStateAdapter
  from rig.engine.ports import RichConfirmationIO

Each test that currently calls apply_plan(...) must now pass state_writer and midi_connection_io. Use:
  state_adapter = InMemoryStateAdapter()
  midi_io = InMemoryMidiConnectionIO(result="confirm", port_name="USB Interface")
  apply_plan(plan, state_writer=state_adapter, midi_connection_io=midi_io, ...)

For tests that previously patched builtins.input to simulate MIDI port selection ("1", then "c"), replace the builtins.input patches with:
  midi_io = InMemoryMidiConnectionIO(result="confirm", port_name="USB Interface")

For tests that previously used a large side_effect list covering MIDI connect + CBA steps + scene steps, the MIDI connection part is now handled by midi_io; the device prompt parts (CBA channel, build_preset, register, scene confirm) are handled by InMemoryPromptAdapter. Construct confirmation_io=InMemoryPromptAdapter(side_effect=[...]) with only the device-interaction inputs (not the MIDI port selection inputs that are now gone). Pass confirmation_io to ApplyContext via apply_plan's internal RichConfirmationIO...

IMPORTANT: The RichConfirmationIO is constructed inside apply_plan (see Task 1). For tests to inject a fake ConfirmationIO, apply_plan must accept it or the test must not rely on it. Re-examine Task 1: the confirmation_io=RichConfirmationIO() is constructed inside apply_plan, which means tests cannot inject a fake for the confirmation prompts via apply_plan's signature.

Correct approach: Add confirmation_io as an optional param to apply_plan with default None, constructing RichConfirmationIO() only when None is passed:

  def apply_plan(
      plan: Plan,
      state_writer: StateWriter,
      midi_connection_io: MidiConnectionIO,
      confirmation_io: ConfirmationIO | None = None,
      ...
  ) -> ApplyResult:
      ...
      ctx = ApplyContext(
          dry_run=dry_run,
          confirmation_io=confirmation_io or RichConfirmationIO(),
          ...
      )

This lets test_apply.py tests pass confirmation_io=InMemoryPromptAdapter(...) while keeping production callers (cli/commands/apply.py) clean with no confirmation_io arg (defaults to RichConfirmationIO).

Update Task 1's apply_plan signature accordingly (it should be an optional param defaulting to None, not a hardcoded construction). Update cli/commands/apply.py if needed (it should NOT pass confirmation_io — let it default).

For test_apply.py, each integration test that previously had a big builtins.input side_effect list should now:
1. Use InMemoryMidiConnectionIO for port selection
2. Use InMemoryPromptAdapter(side_effect=[...]) for the device/CBA interaction steps only (the per-device "c"/"s"/"q" inputs)
3. Pass confirmation_io=fake_prompt_io to apply_plan
4. Use InMemoryStateAdapter and assert on state_adapter.state after apply_plan returns instead of reading the JSON file

For tests that assert on state.json contents after apply_plan, switch to: state = state_adapter.state; assert state.devices["brothers"]... etc.

Remove all `patch("builtins.input", ...)` usage. Keep patches for `rig.midi.adapter.mido.get_output_names` and `rig.midi.adapter.mido.open_output` — those are still needed to avoid real MIDI hardware access.

Remove `patch` from the mock imports in test_apply.py if it becomes unused after all input patches are removed.

After all rewrites, run the full test suite and fix any remaining failures before marking done.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && grep -rn 'patch("builtins.input"' tests/ | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: zero builtins.input patches" || echo "FAIL: {} remaining patches"' && uv run pytest tests/ -q 2>&1 | tail -20</automated>
  </verify>
  <done>grep for patch("builtins.input") in tests/ returns 0 results. uv run pytest tests/ -q exits 0 with all tests passing. No MIDI hardware required to run the test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI boundary → apply_plan | Production adapters constructed at CLI; passed as required params — no global state |
| test boundary → apply_plan | Fakes passed as params; no monkey-patching of module-level callables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | apply_plan state write | mitigate | DEC-06 fix: scene state written only when confirmed; prevents false state persistence for cancelled scenes |
| T-02-04 | Information Disclosure | FileStateWriter | accept | Writes same state.json as the old write_state call; no new data exposure |
| T-02-05 | Tampering | InMemoryStateAdapter in prod | accept | Test-only class; not imported by any production module; no production risk |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `grep -rn 'patch("builtins.input"' tests/` returns 0 results
2. `uv run pytest tests/ -q` exits 0, all tests pass
3. `uv run ruff check src/` exits 0
4. `uv run python -c "import inspect; from rig.engine.apply import apply_plan; sig = inspect.signature(apply_plan); assert 'state_writer' in sig.parameters; assert 'midi_connection_io' in sig.parameters; print('OK')"` exits 0
5. `grep -n "state_writer\|midi_connection_io" src/rig/cli/commands/apply.py` shows both names present
6. `grep -n "state.scenes\[sp.scene_name\]" src/rig/engine/apply.py` shows only one occurrence, inside the `if any(r.status == "confirmed" ...)` block
</verification>

<success_criteria>
- apply_plan signature has required state_writer: StateWriter and midi_connection_io: MidiConnectionIO params (and optional confirmation_io: ConfirmationIO | None = None)
- apply_plan no longer directly calls read_state, write_state, or prompt_midi_connect
- state.scenes[sp.scene_name] = {} only executes when at least one device_result has status == "confirmed"
- cli/commands/apply.py constructs FileStateWriter and InteractiveMidiConnectionIO and passes them to apply_plan
- tests/test_appliers.py has zero patch("builtins.input") calls; _make_ctx passes InMemoryPromptAdapter
- tests/test_apply.py has zero patch("builtins.input") calls; apply_plan called with InMemoryStateAdapter and InMemoryMidiConnectionIO
- Full test suite passes: uv run pytest tests/ exits 0
</success_criteria>

<output>
Create `.planning/phases/02-engine-i-o-decoupling/02-02-SUMMARY.md` when done
</output>
