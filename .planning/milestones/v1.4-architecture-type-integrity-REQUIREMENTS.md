# Requirements Archive: rig-cli v1.4

**Archived:** 2026-06-17
**Milestone:** v1.4 Architecture & Type Integrity
**Outcome:** All 9 requirements satisfied

---

# Requirements: rig-cli v1.4

**Defined:** 2026-06-12
**Core Value:** A single command should bring the physical rig to the exact state described in the config repo — no guessing, no manual knob-hunting.

## v1.4 Requirements

Internal refactor milestone — zero user-visible behavior changes. Eliminates the parallel device model systems, establishes a single typed boundary throughout the plugin/core interface, and closes all test and code-quality debt.

### TYPE — Type System & Model Consolidation

- [x] **TYPE-01**: The codebase has a single `Device` type surface — the legacy `Device(BaseModel)` in `models/device.py` is retired; `Rig.devices` is typed `dict[str, Device]` against the Protocol; all code paths that touch `rig.devices` are type-safe without `Any` or `hasattr` guards
- [x] **TYPE-02**: Each plugin device class carries a concrete config type (`config: ChaseBlissConfig`, `config: HXStompConfig`, etc.) — not `config: Any`; construction from YAML validates against the concrete type
- [x] **TYPE-03**: A `Preset` Protocol exists in `rig.engine.plugin`; all plugin device classes declare `presets: list[Preset]`; the engine no longer sees `list[Any]`
- [x] **TYPE-04**: The dead `plan()` and `diff()` methods are removed from the `Device` Protocol and all plugin implementations (they raise `NotImplementedError` and are never called by the engine)
- [x] **TYPE-05**: `apply.py` has one `ApplyContext` type (the `DeviceApplyContext` dataclass from `engine.plugin`) — the legacy `ApplyContext` dataclass in `appliers/base.py` is retired or clearly scoped; no function passes both types

### TEST — Test Infrastructure

- [x] **TEST-01**: The stale root-level `tests/` directory is deleted — it contains broken imports from a pre-plugin-extraction layout and fails collection with 9 import errors; `make test` runs and passes
- [x] **TEST-02**: The 3 failing tests (`test_build_preset_confirm_…`, `test_midi_connect_and_send`, `test_cba_channel_establishment_…`) pass without `-s` — the interaction functions they exercise route through the `ConfirmationIO` Protocol so test doubles replace `input()` calls

### QUAL — Code Quality

- [x] **QUAL-01**: All raw string device-type comparisons (`== "analog"`, `== "digital"`, `== "controller"`, `== "modeler"`) in engine and plugin code are replaced with `DeviceType` enum members; a grep for these patterns in non-test source returns zero hits
- [x] **QUAL-02**: The TODO comment in `models/device.py` about Enums vs Literals is resolved with a recorded decision: `Literal` is used for Pydantic discriminated union `type` fields (required by Pydantic); `DeviceType` StrEnum is used for runtime comparisons; the two are not in conflict

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| TYPE-01 | Phase 22 | ✅ Complete | `models/device.py` deleted; `Rig.devices` typed against Protocol; zero `hasattr` guards |
| TYPE-02 | Phase 21 | ✅ Complete | All 4 plugins carry concrete config types; VERIFICATION.md written in Phase 24 |
| TYPE-03 | Phase 21 | ✅ Complete | `Preset` Protocol in core; `MC6Device.presets: SkipValidation[list[Preset]]` in Phase 24 |
| TYPE-04 | Phase 20 | ✅ Complete | `plan()`/`diff()` stubs removed from Protocol and all 4 plugin device files |
| TYPE-05 | Phase 23 | ✅ Complete | `appliers/base.py` deleted; `DeviceApplyContext` sole apply context type |
| TEST-01 | Phase 20 | ✅ Complete | Root `tests/` directory deleted; `make test` runs 309 passed, 0 failures |
| TEST-02 | Phase 23 | ✅ Complete | 3 stdin-capture tests pass without `-s`; `ConfirmationIO` threaded through all 4 CBA functions |
| QUAL-01 | Phase 20 | ✅ Complete | Zero raw-string comparisons; `DeviceAction` enum-typed in Phase 24 |
| QUAL-02 | Phase 20 | ✅ Complete | Decision recorded; `models/device.py` subsequently deleted in Phase 22 |

**Coverage:**

- v1.4 requirements: 9 total
- Satisfied: 9 ✓
- Unsatisfied: 0

---
*Requirements defined: 2026-06-12*
*Archived: 2026-06-17 after v1.4 milestone completion*
