---
phase: 29-catalog-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rig-chasebliss/src/rig_chasebliss/catalog.py
  - packages/rig/tests/test_catalog.py
  - packages/rig-chasebliss/tests/test_catalog.py
autonomous: true
requirements:
  - CAT-01
must_haves:
  truths:
    - "`make test` passes with 0 failures"
    - "wet_bypass maps to CC102 and loop_bypass maps to CC103 per Mood MkII MIDI spec"
    - "Both test files assert against named catalog constants, not literal CC integers"
  artifacts:
    - path: packages/rig-chasebliss/src/rig_chasebliss/catalog.py
      provides: "Named constants MOOD_MKII_WET_BYPASS_CC=102 and MOOD_MKII_LOOP_BYPASS_CC=103 at module level; Control entries use those constants"
    - path: packages/rig/tests/test_catalog.py
      provides: "test_knobs_have_correct_cc_numbers asserts against MOOD_MKII_WET_BYPASS_CC and MOOD_MKII_LOOP_BYPASS_CC"
    - path: packages/rig-chasebliss/tests/test_catalog.py
      provides: "test_mood_cc102_named_wet_bypass and test_mood_cc103_named_loop_bypass assert against the named constants"
  key_links:
    - from: packages/rig-chasebliss/src/rig_chasebliss/catalog.py
      to: packages/rig/tests/test_catalog.py
      via: "import MOOD_MKII_WET_BYPASS_CC, MOOD_MKII_LOOP_BYPASS_CC from rig_chasebliss.catalog"
      pattern: "MOOD_MKII_WET_BYPASS_CC|MOOD_MKII_LOOP_BYPASS_CC"
    - from: packages/rig-chasebliss/src/rig_chasebliss/catalog.py
      to: packages/rig-chasebliss/tests/test_catalog.py
      via: "import MOOD_MKII_WET_BYPASS_CC, MOOD_MKII_LOOP_BYPASS_CC from rig_chasebliss.catalog"
      pattern: "MOOD_MKII_WET_BYPASS_CC|MOOD_MKII_LOOP_BYPASS_CC"
---

<objective>
Fix the swapped wet_bypass/loop_bypass CC numbers in catalog.py and eliminate magic numbers in both test files.

Purpose: The catalog is the single authoritative source for CC numbers. Currently wet_bypass=CC103 and loop_bypass=CC102 in the catalog, but the official Mood MkII MIDI spec says wet_bypass=CC102 and loop_bypass=CC103. Two tests fail as a result. After this plan: the catalog is corrected, named module-level constants document the authoritative values, and both test files reference those constants instead of duplicating literal integers.

Output: Corrected catalog.py with two new module-level constants; both test files updated to import and assert against those constants; `make test` passes with 0 failures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-catalog-integrity/CONTEXT.md
@packages/rig-chasebliss/src/rig_chasebliss/catalog.py
@packages/rig/tests/test_catalog.py
@packages/rig-chasebliss/tests/test_catalog.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CC values and add named constants to catalog.py</name>
  <files>packages/rig-chasebliss/src/rig_chasebliss/catalog.py</files>
  <action>
    Add two module-level integer constants directly before the `MOOD_MKII_CONTROLS` list definition. The constants must appear in source before the list so the list can reference them:

      MOOD_MKII_WET_BYPASS_CC: int = 102   # per Mood MkII MIDI Implementation
      MOOD_MKII_LOOP_BYPASS_CC: int = 103  # per Mood MkII MIDI Implementation

    Then update the two Control entries in MOOD_MKII_CONTROLS that currently have the values swapped:
    - The Control with name="wet_bypass" currently has midi_cc=103 — change it to midi_cc=MOOD_MKII_WET_BYPASS_CC
    - The Control with name="loop_bypass" currently has midi_cc=102 — change it to midi_cc=MOOD_MKII_LOOP_BYPASS_CC

    No other entries in MOOD_MKII_CONTROLS are touched. The constants must be exported at module level (no underscore prefix) so test files can import them directly.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && python -c "from rig_chasebliss.catalog import MOOD_MKII_WET_BYPASS_CC, MOOD_MKII_LOOP_BYPASS_CC, MOOD_MKII_CONTROLS; by_name = {c.name: c for c in MOOD_MKII_CONTROLS}; assert MOOD_MKII_WET_BYPASS_CC == 102; assert MOOD_MKII_LOOP_BYPASS_CC == 103; assert by_name['wet_bypass'].midi_cc == 102; assert by_name['loop_bypass'].midi_cc == 103; print('OK')"</automated>
  </verify>
  <done>Constants exist at module level with correct values (102, 103); wet_bypass Control has midi_cc=102; loop_bypass Control has midi_cc=103.</done>
</task>

<task type="auto">
  <name>Task 2: Update both test files to use named catalog constants</name>
  <files>packages/rig/tests/test_catalog.py, packages/rig-chasebliss/tests/test_catalog.py</files>
  <action>
    In packages/rig/tests/test_catalog.py:
    - Add MOOD_MKII_WET_BYPASS_CC and MOOD_MKII_LOOP_BYPASS_CC to the existing import from rig_chasebliss.catalog (the file already imports MOOD_MKII_CONTROLS, Control, ControlType from that module — extend that import line).
    - In TestMoodMkiiCatalog::test_knobs_have_correct_cc_numbers, replace the two hard-coded assertions:
        assert by_name["wet_bypass"].midi_cc == 102  →  assert by_name["wet_bypass"].midi_cc == MOOD_MKII_WET_BYPASS_CC
        assert by_name["loop_bypass"].midi_cc == 103 →  assert by_name["loop_bypass"].midi_cc == MOOD_MKII_LOOP_BYPASS_CC
    - Leave all other assertions in that test method unchanged (time CC14, mix CC15 remain as literals — they have no dedicated constants).

    In packages/rig-chasebliss/tests/test_catalog.py:
    - Add MOOD_MKII_WET_BYPASS_CC and MOOD_MKII_LOOP_BYPASS_CC to the existing import from rig_chasebliss.catalog.
    - In test_mood_cc102_named_wet_bypass, replace the literal key lookup:
        ccs[102].name == "wet_bypass"  →  ccs[MOOD_MKII_WET_BYPASS_CC].name == "wet_bypass"
    - In test_mood_cc103_named_loop_bypass, replace the literal key lookup:
        ccs[103].name == "loop_bypass"  →  ccs[MOOD_MKII_LOOP_BYPASS_CC].name == "loop_bypass"
    - Leave all other tests in this file unchanged.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig/tests/test_catalog.py::TestMoodMkiiCatalog::test_knobs_have_correct_cc_numbers packages/rig-chasebliss/tests/test_catalog.py::test_mood_cc102_named_wet_bypass packages/rig-chasebliss/tests/test_catalog.py::test_mood_cc103_named_loop_bypass -v</automated>
  </verify>
  <done>All three targeted tests pass; no other tests in either file are broken.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green</name>
  <files></files>
  <action>
    Run the full test suite to confirm 0 failures. No code changes in this task — it is a verification gate only.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && make test</automated>
  </verify>
  <done>`make test` exits 0 with 0 failures. The two previously failing tests now pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog.py → test files | Constants are imported; tests are local dev-only, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | catalog.py constant values | accept | Constants are source-controlled; value correctness is enforced by the tests themselves — wrong values cause test failures |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `MOOD_MKII_WET_BYPASS_CC` and `MOOD_MKII_LOOP_BYPASS_CC` are importable from `rig_chasebliss.catalog`
- `MOOD_MKII_WET_BYPASS_CC == 102` and `MOOD_MKII_LOOP_BYPASS_CC == 103`
- No literal `102` or `103` remains in test assertions that reference wet_bypass or loop_bypass
- `make test` exits 0
</verification>

<success_criteria>
1. `make test` passes with 0 failures
2. Both previously failing tests (`test_knobs_have_correct_cc_numbers` in each package) now pass
3. Both test files import and assert against MOOD_MKII_WET_BYPASS_CC and MOOD_MKII_LOOP_BYPASS_CC rather than literal integers
4. catalog.py is the single authoritative source: constants defined once, Control entries reference those constants
</success_criteria>

<output>
Create `.planning/phases/29-catalog-integrity/29-01-SUMMARY.md` when done.
</output>
