---
phase: 33.1-phase-33-apply-01-02-completions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rig/src/rig/engine/apply.py
  - packages/rig/src/rig/cli/commands/apply.py
  - packages/rig/tests/test_apply.py
  - packages/rig/tests/test_cli_apply.py
autonomous: true
requirements:
  - APPLY-01
  - APPLY-02
must_haves:
  truths:
    - "rig apply with an analog device already at the desired preset prints 'already set' and skips the manual prompt"
    - "rig apply --device <id> without --preset applies that device across every scene in the plan"
    - "apply_plan with device_filter set skips controller programming phase"
    - "apply_plan with device_filter does not write state for devices that were not targeted"
    - "make test passes with 0 failures"
  artifacts:
    - path: "packages/rig/src/rig/engine/apply.py"
      provides: "VERIFY branch console.print + device_filter param on apply_plan()"
      contains: "device_filter"
    - path: "packages/rig/src/rig/cli/commands/apply.py"
      provides: "Three-way routing: device+preset, device-only, full plan"
      contains: "elif device and not preset"
    - path: "packages/rig/tests/test_apply.py"
      provides: "TestVerifyDisplay and TestDeviceFilterApply test classes"
      contains: "TestVerifyDisplay"
    - path: "packages/rig/tests/test_cli_apply.py"
      provides: "TestDeviceOnlyApply + updated TestDevicePresetFlagValidation"
      contains: "TestDeviceOnlyApply"
  key_links:
    - from: "packages/rig/src/rig/cli/commands/apply.py"
      to: "packages/rig/src/rig/engine/apply.py:apply_plan"
      via: "device_filter kwarg"
      pattern: "apply_plan.*device_filter"
    - from: "packages/rig/src/rig/engine/apply.py"
      to: "ActionStatus.VERIFY branch"
      via: "console.print already set"
      pattern: "already set"
---

<objective>
Complete two half-shipped apply-engine behaviors: APPLY-01 (display "already set" when VERIFY skip fires) and APPLY-02 (device-only apply across all scenes via device_filter).

Purpose: APPLY-01 silently skips analog devices already at the desired preset — the user sees nothing. APPLY-02 is completely missing; the CLI guard rejects --device without --preset. Both gaps reduce the CLI's usefulness for day-to-day rig management.

Output: Updated apply.py (VERIFY display + device_filter), updated CLI commands/apply.py (three-way routing), new test classes TestVerifyDisplay and TestDeviceFilterApply in test_apply.py, new TestDeviceOnlyApply and updated TestDevicePresetFlagValidation in test_cli_apply.py.
</objective>

<execution_context>
@/Users/derekgliwa/.claude/gsd-core/workflows/execute-plan.md
@/Users/derekgliwa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/rig/src/rig/engine/apply.py
@packages/rig/src/rig/cli/commands/apply.py
@packages/rig/tests/test_apply.py
@packages/rig/tests/test_cli_apply.py
@packages/rig/tests/fakes.py
@packages/rig/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: APPLY-01 — Add "already set" display to VERIFY branch and test it</name>
  <files>packages/rig/src/rig/engine/apply.py, packages/rig/tests/test_apply.py</files>
  <behavior>
    - TestVerifyDisplay.test_verify_action_prints_already_set: build a rig with one analog device (id="tumnus", preset id="edge"); pre-populate state.json via _write_state_file to show last_preset="edge"; compute_plan + apply_plan → capsys.readouterr() must contain "already set", "tumnus", and "edge"
    - TestVerifyDisplay.test_verify_action_prompt_never_called: same setup → InMemoryPromptAdapter.calls must be empty after apply_plan completes
    - TestVerifyDisplay.test_verify_display_on_non_analog_device_also_shows: build a rig with an HX Stomp device at the correct preset in state.json → "already set" appears in captured output (VERIFY fires for non-analog too)
  </behavior>
  <action>
    Write the three failing tests in TestVerifyDisplay first (per D-TDD implicit from research), then add the single console.print line to apply.py to make them pass.

    In apply.py, find the VERIFY branch (currently around line 115-119):
      if action.status == ActionStatus.VERIFY:
          logger.debug(...)
          action_result = DeviceApplyResult(...)
    Insert console.print immediately after the logger.debug line (before action_result assignment):
      console.print(f"  [green]✓[/green] {action.device}: already set to '{action.preset_name}'")

    The console object is the module-level Console() instance already imported in apply.py. Do not create a new Console instance.

    For the test class, follow the existing pattern in test_apply.py:
    - Import AnalogDevice from rig_analog.device, AnalogPreset from rig_analog.preset
    - Use DeviceType.ANALOG for the device type
    - Pass config={"type": "manual"} for the analog device config
    - Use FakeDevice for the controller stub (from tests.conftest)
    - Use _write_state_file(tmp_path, {"devices": {"tumnus": {"last_preset": "edge"}}, "scenes": {}}) to set matching state
    - call compute_plan(rig, root_path=str(tmp_path)) then apply_plan(plan, state_writer=InMemoryStateAdapter(), confirmation_io=InMemoryPromptAdapter(default="confirm"), rig=rig, config_path=str(tmp_path), dry_run=False)
    - capsys.readouterr().out must contain "already set", the device id, and the preset id

    Commit after tests pass: fix(apply): print already-set line in VERIFY branch (APPLY-01)
    Then: test(apply): add TestVerifyDisplay for APPLY-01 output assertion
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig/tests/test_apply.py::TestVerifyDisplay -x -v</automated>
  </verify>
  <done>All three TestVerifyDisplay tests pass; "already set" appears in console output for VERIFY-status actions on both analog and non-analog devices; InMemoryPromptAdapter.calls is empty when state already matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: APPLY-02 — Add device_filter to apply_plan and wire CLI routing</name>
  <files>packages/rig/src/rig/engine/apply.py, packages/rig/src/rig/cli/commands/apply.py, packages/rig/tests/test_apply.py, packages/rig/tests/test_cli_apply.py</files>
  <behavior>
    Engine tests (TestDeviceFilterApply in test_apply.py):
    - test_device_filter_applies_only_named_device: rig with two devices ("klon" analog, "brothers" digital) across two scenes; apply_plan(plan, ..., device_filter="klon") → InMemoryStateAdapter state must contain results only for "klon", not "brothers"
    - test_device_filter_applies_across_multiple_scenes: rig with "klon" in 3 scenes; apply_plan with device_filter="klon" → all 3 scenes' klon action fires (not just scene 1)
    - test_device_filter_skips_controller_phase: rig with controller; apply_plan with device_filter set → FakeDevice.apply() call count on the controller must be 0

    CLI tests (test_cli_apply.py):
    - TestDeviceOnlyApply.test_device_without_preset_routes_to_apply_plan_with_filter: --device klon without --preset → mock verify apply_plan called with device_filter="klon"; exit code 0
    - TestDeviceOnlyApply.test_device_only_unknown_device_exits_1: --device nonexistent without --preset → exit code 1, "not found in rig config" in stdout
    - Update TestDevicePresetFlagValidation.test_device_without_preset_exits_1: this test currently expects exit code 1 and D-05 error text; change it to verify that --device alone exits 0 OR routes correctly (invert the assertion — test must reflect new behavior)
  </behavior>
  <action>
    Write all failing tests first, then implement to make them pass.

    ENGINE CHANGE — apply.py:
    Add `device_filter: str | None = None` as a keyword-only parameter to apply_plan() signature. Inside the per-scene device-action loop, add a continue guard immediately before any result is appended:
      if device_filter and action.device != device_filter:
          continue
    The continue must appear before device_results.append(action_result) so filtered devices never contribute to device_results and never gate the scenes state write.

    Update the controller phase condition to include `and not device_filter`. Find the line (around line 164-165) that reads:
      if rig and rig.controller and not scene:
    Change it to:
      if rig and rig.controller and not scene and not device_filter:

    CLI CHANGE — commands/apply.py:
    Remove the D-05 guard block entirely (lines 43-46, the `if bool(device) != bool(preset):` block). Replace with the three-way routing pattern:

    After the D-04 guard (--scene exclusive check), add device-only existence validation:
      if device and not preset:
          if device not in rig.devices:
              console.print(f"[red]✗[/red] Device '{device}' not found in rig config")
              raise typer.Exit(1)

    Keep the existing D-06 block (device and preset together) intact for the case where both are provided.

    In the try block, change the routing from the current two-branch structure to three branches:
      if device and preset:
          apply_device_preset(device, preset, ...)    # unchanged
      elif device:
          result = compute_plan(rig, root_path=config_path)
          apply_plan(result, state_writer=state_writer, rig=rig, config_path=config_path,
                     dry_run=dry_run, scene=scene, midi=midi, device_filter=device)
      else:
          result = compute_plan(rig, root_path=config_path)
          apply_plan(result, state_writer=state_writer, rig=rig, config_path=config_path,
                     dry_run=dry_run, scene=scene, midi=midi)

    For TestDevicePresetFlagValidation.test_device_without_preset_exits_1: update the test body so the assertion reflects new behavior. The test invokes --device klon without --preset on a minimal rig that has a "klon" device. After APPLY-02, this call is now valid. Patch apply_plan and compute_plan (as MagicMocks) so the test does not actually execute apply logic. Assert exit_code == 0. Also update the docstring from "should exit 1" to "routes to apply_plan with device_filter". Rename the method to test_device_without_preset_routes_apply_plan if desired, but the class name TestDevicePresetFlagValidation stays.

    For test_preset_without_device_exits_1: this test should still expect exit code 1 because --preset without --device remains an error. Update the error message assertion to check for "--preset requires --device" rather than "--device and --preset must be used together" (since the D-05 error text is removed). Actually verify what error message now appears and assert on that text. The D-05 guard removal means the code falls through to the routing block where device=None and preset is set; the elif device branch is skipped; the else branch runs apply_plan with the full plan — which is actually valid behavior for --preset alone. Check whether --preset alone should be rejected or silently ignored. Per RESEARCH.md open question 2: keep --preset alone as exit 1 with updated error. Add a check: `if preset and not device:` before the device-only validation block; print "--preset requires --device" and raise typer.Exit(1).

    Commit order:
    1. test(apply): add TestDeviceFilterApply for APPLY-02 engine behavior
    2. feat(apply): add device_filter param to apply_plan for APPLY-02
    3. test(apply): add TestDeviceOnlyApply CLI tests for APPLY-02
    4. feat(apply): remove D-05 guard, add device-only routing branch (APPLY-02)
    5. test(apply): update TestDevicePresetFlagValidation for new --device behavior
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig/tests/test_apply.py::TestDeviceFilterApply packages/rig/tests/test_cli_apply.py::TestDeviceOnlyApply packages/rig/tests/test_cli_apply.py::TestDevicePresetFlagValidation -x -v</automated>
  </verify>
  <done>TestDeviceFilterApply (3 tests) and TestDeviceOnlyApply (2 tests) all pass. TestDevicePresetFlagValidation passes with updated assertions. apply_plan signature includes device_filter. CLI routes --device-only to apply_plan with device_filter. --preset alone still exits 1.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green gate</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions were introduced. Address any failures before declaring the phase complete. Common failure modes to check:
    - TestVerifyActionSkipped still passes (it was testing the skip behavior, which is unchanged — only display was added)
    - TestApplyPlan.test_clean_plan_prints_no_changes still passes (clean plan returns early before VERIFY branch fires)
    - test_same_scene_applied_twice_no_reprompt_on_second_apply still passes
    - test_device_preset_flags_route_to_apply_device_preset still passes (--device + --preset path unchanged)
    - test_no_flags_uses_existing_apply_plan_path still passes (no-flag path calls apply_plan without device_filter)
    If any test fails due to the APPLY-02 --preset-alone behavior change, ensure the `if preset and not device:` guard is in place in commands/apply.py.

    Commit: chore(apply): phase 33.1 complete — full suite green
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/ -q</automated>
  </verify>
  <done>make test (uv run pytest packages/ -q) exits 0 with 0 failures. All APPLY-01 and APPLY-02 behaviors verified. Phase gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → engine | device_filter value comes from user-supplied --device flag; must validate existence in rig.devices before passing to engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-01 | Tampering | device_filter in apply_plan | mitigate | CLI validates device exists in rig.devices before calling apply_plan; engine silently skips unknown device IDs via the filter loop (no action matched = no state written) |
| T-33.1-02 | Information Disclosure | console.print VERIFY output | accept | Output reveals device id and preset name to the local terminal operator — this is intentional; no sensitive data in device/preset identifiers |
| T-33.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all existing dependencies, no supply chain risk |
</threat_model>

<verification>
1. `uv run pytest packages/rig/tests/test_apply.py::TestVerifyDisplay -x -v` — all 3 APPLY-01 display tests pass
2. `uv run pytest packages/rig/tests/test_apply.py::TestDeviceFilterApply -x -v` — all 3 APPLY-02 engine tests pass
3. `uv run pytest packages/rig/tests/test_cli_apply.py -x -v` — all CLI routing tests pass including updated D-05 test
4. `uv run pytest packages/ -q` — full suite 0 failures

Manual spot-check (optional):
- `rig apply --config <rig-repo> --dry-run` — if any device is at correct state, "already set" line appears
- `rig apply --config <rig-repo> --device <id> --dry-run` — applies only named device across all scenes
</verification>

<success_criteria>
1. `rig apply` with analog device state matching the scene preset: skips manual prompt, prints "  ✓ {device}: already set to '{preset}'" — no prompt adapter call
2. `rig apply --device <id>` without --preset: computes full plan, applies only named device actions across all scenes, does not touch other devices' state, does not fire controller programming phase
3. TestVerifyDisplay (3 tests) and TestDeviceFilterApply (3 tests) in test_apply.py all pass
4. TestDeviceOnlyApply (2 tests) in test_cli_apply.py all pass
5. TestDevicePresetFlagValidation reflects new behavior (--device alone is valid, --preset alone still exits 1)
6. `uv run pytest packages/ -q` exits 0 with 0 failures
</success_criteria>

<output>
Create `.planning/phases/33.1-phase-33-apply-01-02-completions/33.1-01-SUMMARY.md` when done
</output>
