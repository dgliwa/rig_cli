---
phase: 02-engine-i-o-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rig/engine/ports.py
  - tests/fakes.py
  - src/rig/engine/appliers/base.py
  - src/rig/engine/appliers/analog.py
  - src/rig/engine/appliers/midi_device.py
  - src/rig/engine/appliers/chase_bliss.py
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
  - DEC-04

must_haves:
  truths:
    - "ConfirmationIO, StateWriter, and MidiConnectionIO Protocols exist in src/rig/engine/ports.py with production adapters"
    - "ApplyContext has a required confirmation_io field of type ConfirmationIO"
    - "AnalogApplier, MidiApplier, and ChaseBlissApplier route all interaction calls through ctx.confirmation_io"
    - "tests/fakes.py provides InMemoryPromptAdapter, InMemoryStateAdapter, InMemoryMidiConnectionIO"
    - "No applier imports directly from rig.interaction.* for prompts that belong to ConfirmationIO"
  artifacts:
    - path: "src/rig/engine/ports.py"
      provides: "ConfirmationIO, StateWriter, MidiConnectionIO Protocols + production adapters"
      exports:
        - "ConfirmationIO"
        - "StateWriter"
        - "MidiConnectionIO"
        - "RichConfirmationIO"
        - "FileStateWriter"
        - "InteractiveMidiConnectionIO"
    - path: "tests/fakes.py"
      provides: "In-memory test doubles for all three Protocols"
      exports:
        - "InMemoryPromptAdapter"
        - "InMemoryStateAdapter"
        - "InMemoryMidiConnectionIO"
  key_links:
    - from: "src/rig/engine/appliers/analog.py"
      to: "ctx.confirmation_io.prompt_analog"
      via: "ApplyContext.confirmation_io field"
      pattern: "ctx\\.confirmation_io\\.prompt_analog"
    - from: "src/rig/engine/appliers/midi_device.py"
      to: "ctx.confirmation_io.prompt_device"
      via: "ApplyContext.confirmation_io field"
      pattern: "ctx\\.confirmation_io\\.prompt_device"
    - from: "src/rig/engine/appliers/chase_bliss.py"
      to: "ctx.confirmation_io.prompt_cba_"
      via: "ApplyContext.confirmation_io field"
      pattern: "ctx\\.confirmation_io\\.prompt_cba_"
---

<objective>
Create the three I/O Protocol seams and their production adapters, extend ApplyContext with the required confirmation_io field, route all three appliers through it, and provide in-memory fakes for testing.

Purpose: Introduce the structural contracts that let apply_plan and all appliers be tested without MIDI hardware and without patching builtins.input. This is the foundational seam work — nothing production-behavioural changes.

Output: src/rig/engine/ports.py (Protocols + production adapters), tests/fakes.py (in-memory adapters), updated base.py, and three updated applier files with direct interaction imports removed.
</objective>

<execution_context>
@/Users/derekgliwa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/derekgliwa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/derekgliwa/dev/rig-cli/.planning/PROJECT.md
@/Users/derekgliwa/dev/rig-cli/.planning/ROADMAP.md
@/Users/derekgliwa/dev/rig-cli/.planning/phases/02-engine-i-o-decoupling/02-CONTEXT.md
@/Users/derekgliwa/dev/rig-cli/.planning/phases/02-engine-i-o-decoupling/02-PATTERNS.md
@/Users/derekgliwa/dev/rig-cli/src/rig/engine/appliers/base.py
@/Users/derekgliwa/dev/rig-cli/src/rig/interaction/analog.py
@/Users/derekgliwa/dev/rig-cli/src/rig/interaction/midi.py
@/Users/derekgliwa/dev/rig-cli/src/rig/interaction/cba.py
@/Users/derekgliwa/dev/rig-cli/src/rig/engine/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/rig/engine/ports.py with all three Protocols and production adapters (per D-01, D-02, D-03)</name>
  <files>src/rig/engine/ports.py</files>
  <behavior>
    - ConfirmationIO.prompt_analog(device, preset_name) delegates to interaction.analog.prompt_analog and returns its _ConfirmResult
    - ConfirmationIO.prompt_device(device, preset_name, preset_number, midi_channel, midi_connected) delegates to interaction.midi.prompt_device and returns _ConfirmResult
    - ConfirmationIO.prompt_cba_channel(device, midi_channel, midi_sent) delegates to interaction.cba.prompt_cba_channel and returns _ConfirmResult
    - ConfirmationIO.prompt_cba_preset(device, preset_name, preset_number, midi_channel) delegates to interaction.cba.prompt_cba_build_preset and returns _ConfirmResult
    - ConfirmationIO.prompt_cba_register(device, scene_refs) delegates to interaction.cba.prompt_cba_register and returns _ConfirmResult
    - StateWriter.read(root) returns RigState by delegating to engine.state.read_state
    - StateWriter.write(root, state) persists RigState by delegating to engine.state.write_state
    - MidiConnectionIO.prompt_connect(device, midi_channel, midi, device_port) returns tuple[_ConfirmResult, str | None] by delegating to interaction.midi.prompt_midi_connect
  </behavior>
  <action>
Create src/rig/engine/ports.py following the Protocol pattern established in src/rig/engine/appliers/base.py (lines 35-36). File structure:

1. Header: from __future__ import annotations, then imports — typing (Literal, Protocol), rig.engine.state (RigState, read_state, write_state), rig.interaction.analog (prompt_analog), rig.interaction.cba (prompt_cba_channel, prompt_cba_build_preset, prompt_cba_register), rig.interaction.midi (prompt_midi_connect), rig.midi.adapter (MidiManager). Add logger = logging.getLogger(__name__).

2. Define _ConfirmResult = Literal["confirm", "retry", "skip", "quit"] — the single canonical definition for this type, per D-03.

3. Define ConfirmationIO Protocol (per D-01) with five methods using ... bodies (not pass):
   - prompt_analog(self, device: str, preset_name: str) -> _ConfirmResult
   - prompt_device(self, device: str, preset_name: str, preset_number: int | None, midi_channel: int | None, midi_connected: bool) -> _ConfirmResult
   - prompt_cba_channel(self, device: str, midi_channel: int, midi_sent: bool) -> _ConfirmResult
   - prompt_cba_preset(self, device: str, preset_name: str, preset_number: int | None, midi_channel: int) -> _ConfirmResult
   - prompt_cba_register(self, device: str, scene_refs: list[str]) -> _ConfirmResult

4. Define StateWriter Protocol (per D-02) with two methods:
   - read(self, root: str) -> RigState
   - write(self, root: str, state: RigState) -> None

5. Define MidiConnectionIO Protocol (per D-02) with one method (per D-02 — prompt-only, no port opening):
   - prompt_connect(self, device: str, midi_channel: int, midi: MidiManager, device_port: str | None) -> tuple[_ConfirmResult, str | None]

6. Define production adapters as plain classes (NOT @dataclass, NOT BaseModel — these are thin wrappers):
   - RichConfirmationIO — implements ConfirmationIO, each method calls the matching interaction.* function directly with forwarded args
   - FileStateWriter — implements StateWriter, read delegates to read_state(root), write delegates to write_state(root, state)
   - InteractiveMidiConnectionIO — implements MidiConnectionIO, prompt_connect delegates to prompt_midi_connect(device, midi_channel, midi, device_port)

No circular import risk: ports.py imports FROM interaction.* and engine.state; nothing in those modules imports from ports.py.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run python -c "from rig.engine.ports import ConfirmationIO, StateWriter, MidiConnectionIO, RichConfirmationIO, FileStateWriter, InteractiveMidiConnectionIO; print('ports.py imports OK')"</automated>
  </verify>
  <done>ports.py exists and all six names import cleanly. Protocol bodies use ... (not pass). No ABC inheritance. _ConfirmResult defined exactly once in ports.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/fakes.py with InMemoryPromptAdapter, InMemoryStateAdapter, InMemoryMidiConnectionIO (per D-06, D-07)</name>
  <files>tests/fakes.py</files>
  <behavior>
    - InMemoryPromptAdapter(default="confirm", side_effect=None): all five prompt methods return default unless side_effect list provided, in which case they pop in order and fall back to default when exhausted
    - InMemoryPromptAdapter with side_effect=["s", "q"]: first call returns "s", second returns "q", third returns default
    - InMemoryStateAdapter(): read(root) returns current in-memory RigState; write(root, state) replaces it; initial state is RigState()
    - InMemoryMidiConnectionIO(result="confirm", port_name="FakePort"): prompt_connect always returns (result, port_name)
    - InMemoryMidiConnectionIO(result="skip", port_name=None): prompt_connect returns ("skip", None)
  </behavior>
  <action>
Create tests/fakes.py. Start with from __future__ import annotations, then import collections.deque, typing (per need), rig.engine.ports (ConfirmationIO, StateWriter, MidiConnectionIO), rig.engine.state (RigState), rig.midi.adapter (MidiManager) for type hint only (TYPE_CHECKING guard).

InMemoryPromptAdapter (per D-06):
- __init__(self, default: str = "confirm", side_effect: list[str] | None = None): stores default; stores deque(side_effect) if provided else empty deque
- _next(self) -> str: pops left from deque if non-empty, else returns self.default
- All five methods (prompt_analog, prompt_device, prompt_cba_channel, prompt_cba_preset, prompt_cba_register) call self._next() and return its value cast to _ConfirmResult. Method signatures must match the ConfirmationIO Protocol exactly.
- Do NOT record calls — per D-06, no call-count tracking needed.

InMemoryStateAdapter (per D-07):
- __init__(self): self._state = RigState()
- read(self, root: str) -> RigState: return self._state (ignore root — in-memory only)
- write(self, root: str, state: RigState) -> None: self._state = state
- Property state -> RigState: return self._state (lets tests assert on final state without calling read)

InMemoryMidiConnectionIO:
- __init__(self, result: str = "confirm", port_name: str | None = "FakePort"): store both
- prompt_connect(self, device, midi_channel, midi, device_port) -> tuple: return (self.result, self.port_name)

Style: plain classes, no @dataclass, no MagicMock. Matches project convention (see PATTERNS.md §tests/fakes.py).
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run python -c "from tests.fakes import InMemoryPromptAdapter, InMemoryStateAdapter, InMemoryMidiConnectionIO; f = InMemoryPromptAdapter(default='skip', side_effect=['c','q']); assert f.prompt_analog('x','y') == 'c'; assert f.prompt_device('x','y',None,None,False) == 'q'; assert f.prompt_analog('x','y') == 'skip'; print('fakes.py OK')"</automated>
  </verify>
  <done>tests/fakes.py exists; side_effect pops in order and falls back to default; InMemoryStateAdapter round-trips a RigState without touching disk; InMemoryMidiConnectionIO returns configured values.</done>
</task>

<task type="auto">
  <name>Task 3: Add confirmation_io to ApplyContext and route all three appliers through it (per D-04)</name>
  <files>
    src/rig/engine/appliers/base.py
    src/rig/engine/appliers/analog.py
    src/rig/engine/appliers/midi_device.py
    src/rig/engine/appliers/chase_bliss.py
  </files>
  <action>
base.py changes (per D-04):
- Add import: from rig.engine.ports import ConfirmationIO (real import, not TYPE_CHECKING-guarded — it is a runtime field).
- Add field to ApplyContext dataclass: confirmation_io: ConfirmationIO (required, no default). Place it after the existing dry_run field and before the optional fields (midi, connected_devices, state, config_path, rig). Being required means it has no default= or field() — it must be passed at construction.
- No other changes to base.py.

analog.py changes:
- Remove: from rig.interaction.analog import prompt_analog
- Replace the call at line 30: res = prompt_analog(action.device, action.preset_name) with res = ctx.confirmation_io.prompt_analog(action.device, action.preset_name)

midi_device.py changes:
- Remove: from rig.interaction.midi import prompt_device
- Replace the call at line 53 (prompt_device(...)) with: result = ctx.confirmation_io.prompt_device(action.device, action.preset_name, action.preset_number, action.midi_channel, midi_connected=midi_connected)

chase_bliss.py changes:
- Remove: from rig.interaction.cba import prompt_cba_build_preset, prompt_cba_channel, prompt_cba_register
- Replace prompt_cba_channel call (line ~99): res = ctx.confirmation_io.prompt_cba_channel(action.device, action.midi_channel, midi_sent=False) and the second call after midi_sent=True is set: res = ctx.confirmation_io.prompt_cba_channel(action.device, action.midi_channel, midi_sent=True) — preserve the existing while-True retry loop structure.
- Replace prompt_cba_build_preset call (line ~180): res = ctx.confirmation_io.prompt_cba_preset(action.device, action.preset_name, action.preset_number, action.midi_channel)
- Replace prompt_cba_register call: res = ctx.confirmation_io.prompt_cba_register(action.device, action.scene_refs)

CRITICAL: confirmation_io is now required on ApplyContext. Every ApplyContext construction site must pass confirmation_io. In this plan's scope, the only construction sites are in tests (handled in plan 02-02). The apply.py construction site is also handled in plan 02-02. Do NOT add a default here.

After editing, run the linter to catch any remaining direct imports from interaction.* in the applier files.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run ruff check src/rig/engine/appliers/ && uv run python -c "from rig.engine.appliers.analog import AnalogApplier; from rig.engine.appliers.midi_device import MidiApplier; from rig.engine.appliers.chase_bliss import ChaseBlissApplier; print('applier imports OK')" && grep -rn "from rig.interaction" src/rig/engine/appliers/ | grep -v "^Binary" && echo "NOTE: above should be empty or only non-prompt imports"</automated>
  </verify>
  <done>base.py has confirmation_io: ConfirmationIO as the first required field after dry_run. All three appliers call ctx.confirmation_io.* instead of importing from rig.interaction.*. Ruff reports no errors. No prompt-related imports remain in any of the three applier files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production ports | Tests use in-memory fakes that implement the same Protocols as production adapters; no trust boundary crossing |
| ports.py → interaction.* | Production adapters delegate to existing functions; no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | InMemoryStateAdapter | accept | In-memory only; no disk writes; test-only class never ships in production bundle |
| T-02-02 | Tampering | ports.py production adapters | accept | Adapters are thin wrappers; no logic change; existing interaction.* functions unchanged |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all dependencies already in pyproject.toml |
</threat_model>

<verification>
1. `uv run python -c "from rig.engine.ports import ConfirmationIO, StateWriter, MidiConnectionIO, RichConfirmationIO, FileStateWriter, InteractiveMidiConnectionIO"` exits 0
2. `uv run python -c "from tests.fakes import InMemoryPromptAdapter, InMemoryStateAdapter, InMemoryMidiConnectionIO"` exits 0
3. `grep -rn "from rig.interaction.analog import" src/rig/engine/appliers/` returns no results
4. `grep -rn "from rig.interaction.midi import prompt_device" src/rig/engine/appliers/` returns no results
5. `grep -rn "from rig.interaction.cba import" src/rig/engine/appliers/` returns no results
6. `uv run ruff check src/rig/engine/` exits 0
</verification>

<success_criteria>
- ports.py defines ConfirmationIO (5 methods), StateWriter (2 methods), MidiConnectionIO (1 method) as Protocol classes with ... bodies
- ports.py defines RichConfirmationIO, FileStateWriter, InteractiveMidiConnectionIO as concrete adapters delegating to existing interaction.* functions
- tests/fakes.py defines InMemoryPromptAdapter with side_effect queue and default fallback, InMemoryStateAdapter with in-memory RigState, InMemoryMidiConnectionIO with configurable return values
- ApplyContext.confirmation_io: ConfirmationIO is a required field (no default)
- Zero direct imports of prompt_analog, prompt_device, prompt_cba_* from rig.interaction.* in any of the three applier files
- uv run ruff check src/ exits 0
</success_criteria>

<output>
Create `.planning/phases/02-engine-i-o-decoupling/02-01-SUMMARY.md` when done
</output>
