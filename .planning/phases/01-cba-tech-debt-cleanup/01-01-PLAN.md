---
phase: 01-cba-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rig/engine/appliers/base.py
  - src/rig/engine/plan.py
  - src/rig/engine/appliers/chase_bliss.py
autonomous: true
requirements: [CBA-01, CBA-02, CBA-03]

must_haves:
  truths:
    - "All presets_saved updates in ChaseBlissApplier flow through one named helper, not raw dict construction"
    - "ChaseBlissApplier imports detect_cba_setup (public, no leading underscore) from plan.py"
    - "No private symbol (_name) is referenced across module boundaries anywhere in the engine package"
    - "Existing ChaseBlissApplier and apply_plan tests pass unchanged after the refactor (behavior-neutral)"
  artifacts:
    - path: "src/rig/engine/appliers/base.py"
      provides: "mark_preset_saved(state, device, preset_id) helper that delegates to update_device_state"
      contains: "def mark_preset_saved"
    - path: "src/rig/engine/plan.py"
      provides: "detect_cba_setup public function with _is_cba inlined"
      contains: "def detect_cba_setup"
    - path: "src/rig/engine/appliers/chase_bliss.py"
      provides: "_build_preset and _enqueue_new_actions using public symbols only"
      contains: "mark_preset_saved"
  key_links:
    - from: "src/rig/engine/appliers/chase_bliss.py"
      to: "mark_preset_saved"
      via: "import from rig.engine.appliers.base"
      pattern: "from rig.engine.appliers.base import.*mark_preset_saved"
    - from: "src/rig/engine/appliers/chase_bliss.py"
      to: "detect_cba_setup"
      via: "import from rig.engine.plan"
      pattern: "from rig.engine.plan import.*detect_cba_setup"
---

<objective>
Remove the three CBA tech-debt items in `ChaseBlissApplier` before Phase 2 protocol work begins: raw `presets_saved` dict mutation and two private-symbol cross-module imports. This is a behavior-neutral refactor — no new behavior, no new features, no new tests (per D-02).

Three changes:
1. Add `mark_preset_saved(state, device, preset_id)` helper to `base.py`; route `_build_preset`'s `presets_saved` update through it (CBA-01).
2. Rename `_detect_cba_setup` → `detect_cba_setup` (public) in `plan.py`; update both call sites in `plan.py` and the import + call site in `chase_bliss.py` (CBA-02).
3. Inline `_is_cba` directly into `detect_cba_setup` and delete the symbol — it is called exactly once and never referenced outside `plan.py` (CBA-03, per D-01).

Purpose: Establish a clean module seam so Phase 2 can introduce Protocol-typed I/O ports without inheriting private-symbol leakage. A single auditable state-write path also prepares for the `StateWriter` Protocol in Phase 2.
Output: Three modified files; `mark_preset_saved` helper; public `detect_cba_setup`; no `_is_cba` symbol; existing test suite green.
</objective>

<artifacts_this_phase_produces>
New symbols introduced by this phase (exclude from drift verification):
- `mark_preset_saved` — new function in `src/rig/engine/appliers/base.py`, signature `mark_preset_saved(state: RigState, device: str, preset_id: str) -> None`
- `detect_cba_setup` — public function in `src/rig/engine/plan.py` (renamed from `_detect_cba_setup`; same signature `detect_cba_setup(rig: Rig, state: RigState) -> list[CbaSetupAction]`)

Symbols removed by this phase:
- `_detect_cba_setup` — renamed to `detect_cba_setup` (no longer exists)
- `_is_cba` — inlined and deleted (no longer exists)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cba-tech-debt-cleanup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mark_preset_saved helper to base.py</name>
  <files>src/rig/engine/appliers/base.py</files>
  <read_first>
    - src/rig/engine/appliers/base.py — see existing `update_device_state(state, device, **fields)` at line 39 (the helper `mark_preset_saved` must delegate to, NOT bypass) and the imports of `DeviceState`, `RigState` at line 8
    - src/rig/engine/state.py — `DeviceState.presets_saved: dict[str, bool] = {}` at line 22 (the field being updated); `RigState.devices: dict[str, DeviceState]` at line 29
    - src/rig/engine/appliers/chase_bliss.py — lines 209-212 show the raw mutation pattern (`ds.presets_saved` read → copy dict → set key → `model_copy`) this helper replaces; replicate its semantics exactly
  </read_first>
  <action>
    Add a new module-level function `mark_preset_saved(state: RigState, device: str, preset_id: str) -> None` to `src/rig/engine/appliers/base.py`, placed immediately after `update_device_state` (currently the last function, line 39-42).

    Per D-03, the helper MUST compose with the existing `update_device_state` rather than duplicate the `model_copy` pattern. Implementation contract:
    - Read the current `DeviceState` for `device` via `state.devices.get(device, DeviceState())` (same default-construct fallback `update_device_state` uses).
    - Build an updated `presets_saved` dict from `dict(current.presets_saved)` with `preset_id` set to `True` (do not mutate `current.presets_saved` in place — Pydantic immutable-update pattern, per CONTEXT.md Established Patterns).
    - Call `update_device_state(state, device, presets_saved=<updated_dict>)` to perform the actual write — this keeps a single code path for all state mutations.
    - Return `None`.

    `DeviceState` and `RigState` are already imported at line 8 — no new imports needed. Match the existing annotation style (`from __future__ import annotations` is at line 1; use the `str`/`dict` lowercase-generic conventions already in the file). Give it a one-line docstring describing that it marks one preset as saved through the single state-write path.

    Do NOT change the existing `update_device_state` signature or behavior.
  </action>
  <verify>
    <automated>uv run python -c "from rig.engine.appliers.base import mark_preset_saved; from rig.engine.state import RigState; s=RigState(); mark_preset_saved(s,'dev','p1'); assert s.devices['dev'].presets_saved=={'p1':True}; mark_preset_saved(s,'dev','p2'); assert s.devices['dev'].presets_saved=={'p1':True,'p2':True}, s.devices['dev'].presets_saved; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def mark_preset_saved" src/rig/engine/appliers/base.py` returns exactly one match with signature `def mark_preset_saved(state: RigState, device: str, preset_id: str) -> None`.
    - The body of `mark_preset_saved` contains a call to `update_device_state(` with a `presets_saved=` keyword argument (verifiable: `grep -A8 "def mark_preset_saved" src/rig/engine/appliers/base.py | grep "update_device_state"` matches).
    - The verify command prints `OK` — calling the helper twice on the same device accumulates keys (`{'p1': True, 'p2': True}`) rather than overwriting, proving it merges into existing `presets_saved`.
    - `uv run ruff check src/rig/engine/appliers/base.py` reports no errors.
  </acceptance_criteria>
  <done>`mark_preset_saved` exists in `base.py`, delegates to `update_device_state`, accumulates preset keys without overwriting, and passes ruff.</done>
</task>

<task type="auto">
  <name>Task 2: Make detect_cba_setup public, inline _is_cba, and route preset save through mark_preset_saved</name>
  <files>src/rig/engine/plan.py, src/rig/engine/appliers/chase_bliss.py</files>
  <read_first>
    - src/rig/engine/plan.py — `_is_cba` at line 64-68 (the predicate to inline), `_detect_cba_setup` at line 71-125 (rename target; line 76 has the `if not _is_cba(pedal): continue` call to inline), and the second call site at line 220 (`cba_setup = _detect_cba_setup(rig, actual)`)
    - src/rig/engine/appliers/chase_bliss.py — line 9 import (`from rig.engine.plan import CbaSetupAction, DeviceAction, _detect_cba_setup`), line 40 call site (`for a in _detect_cba_setup(ctx.rig, ctx.state):`), line 7 existing base import, and lines 208-212 (the raw `presets_saved` mutation block in `_build_preset` to replace)
    - src/rig/engine/appliers/base.py — confirm `mark_preset_saved` signature from Task 1 (`mark_preset_saved(state, device, preset_id)`) before importing it
    - tests/test_appliers.py — line 302 asserts `ctx.state.devices["cba-mood"].presets_saved.get("shimmer") is True` (behavior this refactor must preserve)
    - tests/test_apply.py — line 296 asserts `state["devices"]["brothers"]["presets_saved"]["low-gain"] is True` (behavior this refactor must preserve)
  </read_first>
  <action>
    Three edits across two files. All are behavior-neutral.

    **Edit A — plan.py: rename `_detect_cba_setup` → `detect_cba_setup` (CBA-02).**
    Rename the function definition at line 71 to `def detect_cba_setup(rig: Rig, state: RigState) -> list[CbaSetupAction]:`. Update the internal call site at line 220 from `_detect_cba_setup(rig, actual)` to `detect_cba_setup(rig, actual)`. Keep the existing `# TODO: This shouldn't be here` comment above line 220 unchanged.

    **Edit B — plan.py: inline `_is_cba` and delete it (CBA-03, per D-01).**
    Delete the entire `_is_cba` function (lines 64-68). At the former line 76 inside `detect_cba_setup`, replace `if not _is_cba(pedal):` with an inlined isinstance check on the pedal's config against `ChaseBlissConfig`. Move the `from rig.models.pedal import ChaseBlissConfig` import (currently inside `_is_cba` at line 66) to inside `detect_cba_setup` (a function-local import is fine and preserves the existing lazy-import pattern that avoids circular imports — keep it function-local, do NOT promote to module top-level). The inlined predicate must be `isinstance(pedal.config, ChaseBlissConfig)` so the `if not isinstance(pedal.config, ChaseBlissConfig): continue` guard behaves identically to the old `if not _is_cba(pedal): continue`. After this edit, `_is_cba` must not appear anywhere in `plan.py`.

    **Edit C — chase_bliss.py: update imports/call sites and route preset save through the helper (CBA-02 wiring + CBA-01).**
    1. Line 9: change `from rig.engine.plan import CbaSetupAction, DeviceAction, _detect_cba_setup` to import `detect_cba_setup` (no underscore).
    2. Line 7: add `mark_preset_saved` to the existing import `from rig.engine.appliers.base import ApplyContext, DeviceApplyResult, update_device_state` so it becomes `... import ApplyContext, DeviceApplyResult, mark_preset_saved, update_device_state` (keep alphabetical/existing order conventions of the file).
    3. Line 40: change `for a in _detect_cba_setup(ctx.rig, ctx.state):` to `for a in detect_cba_setup(ctx.rig, ctx.state):`.
    4. Lines 209-212: replace the three-line raw mutation block (`ds = ctx.state.devices.get(...)` / `ps = dict(ds.presets_saved)` / `ps[action.preset_id] = True` / `ctx.state.devices[...] = ds.model_copy(...)`) with a single call `mark_preset_saved(ctx.state, action.device, action.preset_id)`. Keep the preceding `update_device_state(ctx.state, action.device, last_preset=action.preset_name)` at line 208 unchanged — only the `presets_saved` block is replaced. After this edit, no raw `presets_saved` dict assignment remains in `chase_bliss.py`.

    The `DeviceState` import at line 10 of chase_bliss.py is still used elsewhere (line 209 was the consumer being removed — verify whether `DeviceState` is still referenced after Edit C; if it is no longer used anywhere in the file, remove the now-unused import to keep ruff clean; if it is still used, leave it).
  </action>
  <verify>
    <automated>uv run pytest tests/test_appliers.py tests/test_apply.py tests/test_plan.py -q && uv run ruff check src/rig/engine/plan.py src/rig/engine/appliers/chase_bliss.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_detect_cba_setup" src/` returns zero matches (old private name fully removed).
    - `grep -rn "_is_cba" src/` returns zero matches (symbol inlined and deleted).
    - `grep -c "def detect_cba_setup" src/rig/engine/plan.py` returns `1` (public function exists).
    - `grep -n "isinstance(pedal.config, ChaseBlissConfig)" src/rig/engine/plan.py` returns at least one match (predicate inlined into detect_cba_setup).
    - `grep -E "from rig.engine.plan import" src/rig/engine/appliers/chase_bliss.py` shows `detect_cba_setup` and does NOT contain `_detect_cba_setup`.
    - `grep -n "mark_preset_saved" src/rig/engine/appliers/chase_bliss.py` shows the helper imported and called in `_build_preset`.
    - `grep -nE "presets_saved.*=|\.model_copy\(update=\{.preset" src/rig/engine/appliers/chase_bliss.py` returns no raw `presets_saved` dict-assignment lines (the only `presets_saved` reference should be inside the `mark_preset_saved` call, not a dict mutation).
    - `uv run pytest tests/test_appliers.py tests/test_apply.py tests/test_plan.py -q` reports `53 passed` (same count as the pre-refactor baseline — proves behavior-neutrality, satisfying D-02).
    - `uv run ruff check src/rig/engine/plan.py src/rig/engine/appliers/chase_bliss.py` reports no errors (no unused imports left behind).
  </acceptance_criteria>
  <done>`detect_cba_setup` is public and called from all three sites with no underscore; `_is_cba` no longer exists (inlined); `_build_preset` saves presets via `mark_preset_saved`; no raw `presets_saved` mutation remains; all 53 existing tests pass and ruff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is a behavior-neutral internal refactor. No trust boundary is added, moved, or removed:
- No new user input is parsed (no new CLI flags, no new config fields).
- No new network or MIDI calls are introduced — `mark_preset_saved` only touches the in-memory `RigState` already held by the process; MIDI I/O paths in `_build_preset` are untouched.
- No new file reads/writes — state serialization (`write_state`) is unchanged; this phase only changes how the in-memory `presets_saved` dict is constructed before that existing write.
- No new dependencies, no package installs (no npm/pip/cargo changes) — Package Legitimacy Gate not applicable.

The attack surface is NOT expanded by this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `mark_preset_saved` state write | accept | Writes only to in-process `RigState.presets_saved` with a literal `True` value derived from internal action data, not user input; no new tainted data path. Behavior parity enforced by the existing 53-test suite. |
| T-01-02 | Tampering | Symbol rename / inline across modules | accept | Pure rename + inline; verified by grep gates (zero `_detect_cba_setup`/`_is_cba`) and the unchanged test suite. No runtime behavior or data flow changes. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; dependency set unchanged. No legitimacy checkpoint required. |
</threat_model>

<verification>
Phase-level checks (run from repo root):

1. No private-symbol leakage anywhere in engine package:
   `grep -rn "_detect_cba_setup\|_is_cba" src/rig/engine/` returns zero matches.

2. Single auditable state-write path:
   `grep -rn "presets_saved" src/rig/engine/appliers/chase_bliss.py` shows the value referenced only via the `mark_preset_saved` call (no raw dict assignment).

3. Behavior-neutrality (D-02): full suite stays green:
   `make test` (or `uv run pytest tests/ -q`) passes with no test count regression.

4. Lint clean:
   `make lint` (or `uv run ruff check src/`) reports no errors.
</verification>

<success_criteria>
Maps directly to ROADMAP Phase 1 success criteria:

1. **CBA-01:** `_build_preset` calls `mark_preset_saved(ctx.state, action.device, action.preset_id)` — no raw `state.devices[...].presets_saved` dict assignment remains in `chase_bliss.py`.
2. **CBA-02:** `detect_cba_setup` is a public symbol in `plan.py`; `ChaseBlissApplier` imports it without a leading underscore (line 9) and calls it without underscore (line 40); `plan.py`'s own call site (line 220) uses the public name.
3. **CBA-03:** `_is_cba` is inlined into `detect_cba_setup` (per D-01) and the symbol is deleted — `grep -rn "_is_cba" src/` returns nothing.
4. No private symbol (`_name`) is referenced across module boundaries anywhere in the engine package.
5. Existing test suite passes unchanged (53 tests in the affected modules), proving the refactor is behavior-neutral.
</success_criteria>

<output>
Create `.planning/phases/01-cba-tech-debt-cleanup/01-01-SUMMARY.md` when done.
</output>
