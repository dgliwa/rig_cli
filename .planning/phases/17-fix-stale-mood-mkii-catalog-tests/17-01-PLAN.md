---
phase: 17-fix-stale-mood-mkii-catalog-tests
plan: 01
type: execute
wave: 1
depends_on:
  - 16-reset-to-defaults
files_modified:
  - packages/rig/tests/test_catalog.py
autonomous: true
requirements:
  - CBA-03

must_haves:
  truths:
    - "TestMoodMkiiCatalog::test_has_expected_knob_count asserts 13 knobs (not 7)"
    - "TestMoodMkiiCatalog::test_knobs_have_correct_cc_numbers asserts wet_bypass.midi_cc == 102 (not 103)"
    - "TestMoodMkiiCatalog::test_knobs_have_correct_cc_numbers asserts loop_bypass.midi_cc == 103 (not micro_bypass)"
    - "uv run pytest -q exits 0 — all 296 tests pass, 0 failures"
  artifacts:
    - path: "packages/rig/tests/test_catalog.py"
      provides: "Updated TestMoodMkiiCatalog assertions"
      contains: "len(knobs) == 13 and loop_bypass.midi_cc == 103"
---

<objective>
Fix two stale assertions in `packages/rig/tests/test_catalog.py::TestMoodMkiiCatalog` that
were not updated when Phase 14 expanded the Mood MkII catalog.

Root cause: Phase 14 added 6 new KNOB controls (CC24-29: stereo_width, ramping_waveform, fade,
tone, level_balance, direct_micro_loop) and renamed CC102→wet_bypass / CC103→loop_bypass. The
two tests still assert the pre-Phase-14 values (7 knobs, wrong CC assignments).

The full test suite currently fails 4 tests because pytest's importlib module collection picks
up packages/rig/tests/test_catalog.py once for each of the two test packages that have a
test_catalog.py module, so the 2 unique assertion failures appear 4 times in the summary.
Fixing the 2 assertions resolves all 4 reported failures.

No source code changes; no new tests needed — `packages/rig-chasebliss/tests/test_catalog.py`
already has comprehensive Phase-14 catalog coverage that passes when run in isolation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@packages/rig/tests/test_catalog.py
@packages/rig-chasebliss/src/rig_chasebliss/catalog.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix stale TestMoodMkiiCatalog assertions</name>
  <files>packages/rig/tests/test_catalog.py</files>
  <read_first>
    - packages/rig/tests/test_catalog.py (read the TestMoodMkiiCatalog class — lines ~14-33)
    - packages/rig-chasebliss/src/rig_chasebliss/catalog.py (count KNOB controls in
      MOOD_MKII_CONTROLS; verify CC102 name and CC103 name)
  </read_first>
  <action>
Edit `packages/rig/tests/test_catalog.py` — two changes inside `TestMoodMkiiCatalog`:

**Change 1 — test_has_expected_knob_count:**
The catalog now has 13 KNOB-type controls (CC14-20 original 7, plus CC24-29 added in Phase 14).

    # Before
    assert len(knobs) == 7

    # After
    assert len(knobs) == 13

**Change 2 — test_knobs_have_correct_cc_numbers:**
Phase 14 renamed CC102 to `wet_bypass` and CC103 to `loop_bypass`. The old name `micro_bypass`
does not exist in the catalog.

    # Before
    assert by_name["wet_bypass"].midi_cc == 103
    assert by_name["micro_bypass"].midi_cc == 102

    # After
    assert by_name["wet_bypass"].midi_cc == 102
    assert by_name["loop_bypass"].midi_cc == 103

Do not touch any other tests in the file.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig/tests/test_catalog.py::TestMoodMkiiCatalog -v</automated>
  </verify>
  <done>
    Both TestMoodMkiiCatalog tests pass; 0 failures in packages/rig/tests/test_catalog.py.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full test suite green check</name>
  <files>(none — verification only)</files>
  <read_first>(none)</read_first>
  <action>
Run the full test suite to confirm all 4 previously-failing tests now pass and no regressions
were introduced:

    uv run pytest -q

Expected outcome: all tests pass, 0 failures, 0 errors.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    pytest exits 0; summary line shows N passed (no failures, no errors).
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `uv run pytest -q` — exits 0, no failures.
2. `grep "len(knobs) == 13" packages/rig/tests/test_catalog.py` — returns 1 match.
3. `grep "loop_bypass" packages/rig/tests/test_catalog.py` — returns 1 match.
4. `grep "micro_bypass" packages/rig/tests/test_catalog.py` — returns 0 matches (old name removed).
</verification>

<success_criteria>
- `uv run pytest -q` exits 0 — all tests pass, 0 failures.
- `test_has_expected_knob_count` asserts 13 knobs (matches current MOOD_MKII_CONTROLS).
- `test_knobs_have_correct_cc_numbers` uses `wet_bypass` (CC102) and `loop_bypass` (CC103).
- No other tests modified or deleted.
</success_criteria>

<output>
Create `.planning/phases/17-fix-stale-mood-mkii-catalog-tests/17-01-SUMMARY.md` when done.
</output>
