---
phase: 16-reset-to-defaults
plan: 01
type: execute
wave: 1
depends_on:
  - 15-preset-parameter-validation
files_modified:
  - packages/rig-chasebliss/src/rig_chasebliss/applier.py
  - packages/rig-chasebliss/tests/test_applier.py
autonomous: true
requirements:
  - CBA-06

must_haves:
  truths:
    - "_build_preset sends reset CC batch before preset CC batch when there are controls with non-None defaults"
    - "Reset CC batch includes only controls where control.default is not None AND control.midi_cc is not None"
    - "Footswitch and utility controls (default=None) are excluded from reset batch — no spurious CC messages"
    - "Reset fires per-preset, immediately before that preset's CC messages, not once per apply session"
    - "Dry-run prints reset CC count alongside existing CC param output"
    - "Individual CC send failures during reset are logged and do NOT block preset CCs from being sent"
    - "uv run pytest packages/rig-chasebliss/ -q passes"
  artifacts:
    - path: "packages/rig-chasebliss/src/rig_chasebliss/applier.py"
      provides: "Reset-to-defaults in _build_preset"
      contains: "reset CC batch sent before preset CC batch"
    - path: "packages/rig-chasebliss/tests/test_applier.py"
      provides: "Reset behavior tests"
      contains: "test_reset_defaults_*"
---

<objective>
Add reset-to-defaults step inside `ChaseBlissApplier._build_preset()` in
`packages/rig-chasebliss/src/rig_chasebliss/applier.py`. Before sending each preset's CC
parameter values, send a batch of CC messages to drive all resettable controls (those with
a non-`None` catalog default) to their factory default values.

D-01: Reset inline in `_build_preset` — no new action type (D-01).
D-02: Unconditional per-preset — every `build_preset` invocation sends defaults first (D-02).
D-03: Reset CC computed at apply time from `device.config.controls` (D-03).
D-04: Filter: `control.default is not None and control.midi_cc is not None`; format per reset CC:
      `{"cc": <midi_cc>, "value": int(<default>)}` (D-04).
D-05: Dry-run prints `"  [dim]→ reset {N} defaults before {M} CC params[/dim]"` (D-05).
D-06: Individual CC failures logged but non-blocking — same pattern as existing `_send_ccs()` (D-06).

Output:
- `applier.py` — `_build_preset()` computes reset CC batch from `device.config.controls`,
  sends reset CCs before preset CCs, handles dry-run and failure logging.
- `tests/test_applier.py` — behavioral tests for reset integration: dry-run output format,
  reset CC filter logic, per-preset frequency, failure non-blocking, integration with
  existing CC param send.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-reset-to-defaults/16-CONTEXT.md
@packages/rig-chasebliss/src/rig_chasebliss/applier.py
@packages/rig-chasebliss/src/rig_chasebliss/device.py
@packages/rig-chasebliss/src/rig_chasebliss/catalog.py
@packages/rig-chasebliss/src/rig_chasebliss/models.py
@packages/rig/src/rig/engine/appliers/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reset-to-defaults inside _build_preset</name>
  <files>packages/rig-chasebliss/src/rig_chasebliss/applier.py</files>
  <read_first>
    - packages/rig-chasebliss/src/rig_chasebliss/applier.py (full file — understand
      _build_preset structure, _send_ccs inner function pattern, dry-run output format,
      and how action.cc_params is used)
    - packages/rig-chasebliss/src/rig_chasebliss/device.py (ChaseBlissConfig with controls
      list — the source for reset CC computation)
    - packages/rig-chasebliss/src/rig_chasebliss/catalog.py (Control model — note default
      field, midi_cc field, and how to iterate controls with non-None defaults)
  </read_first>
  <action>
Modify `_build_preset` in `packages/rig-chasebliss/src/rig_chasebliss/applier.py` to add
a reset-to-defaults step before the preset CC params are sent.

Changes:

1. **Add a helper function `_send_reset_ccs()` inside `_build_preset`**, placed immediately
   before the existing `_send_ccs()` inner function. Use the same pattern as `_send_ccs()`:

       def _send_reset_ccs() -> int:
           """Send CC messages for all resettable controls to their catalog defaults."""
           sent = 0
           device = _find_device(ctx.rig, action.device)
           if device is None or not hasattr(device.config, 'controls'):
               return sent
           resettable = [
               c for c in device.config.controls
               if c.default is not None and c.midi_cc is not None
           ]
           if not resettable:
               return sent
           if ctx.midi is None:
               return sent
           for control in resettable:
               try:
                   ctx.midi.send_control_change(
                       action.device, control.midi_cc, int(control.default), action.midi_channel
                   )
                   sent += 1
               except Exception as e:
                   logger.error("Reset CC %d failed on %s: %s", control.midi_cc, action.device, e)
                   console.print(f"  [red]✗[/red] Reset CC send failed (CC {control.midi_cc}): {e}")
           return sent

2. **Add a module-level helper `_find_device`** at the top of the module (after the imports,
   before the class):

       def _find_device(rig, device_id: str):
           """Find a device by ID in the rig, or return None."""
           if rig is None:
               return None
           return next((d for d in rig.devices if d.id == device_id), None)

3. **In the dry-run branch of `_build_preset`**, after the existing dry-run print, compute
   the resettable count and include it:

       device = _find_device(ctx.rig, action.device)
       reset_count = 0
       if device is not None and hasattr(device.config, 'controls'):
           reset_count = len([
               c for c in device.config.controls
               if c.default is not None and c.midi_cc is not None
           ])
       cc_count = len(action.cc_params)
       if reset_count:
           console.print(
               f"  [dim]→ reset {reset_count} defaults before {cc_count} CC params[/dim]"
           )

   The existing `cc_count = len(action.cc_params)` line should be moved before this new
   block (it is later used for the existing dry-run print which should remain unchanged
   except for the addition of the reset line).

4. **In the live branch** (inside the `if ctx.dry_run:` ... `else:` path, which is implicit),
   call `_send_reset_ccs()` immediately before `_send_ccs()`:

       def _build_preset(self, action: CbaSetupAction, ctx: ApplyContext) -> DeviceApplyResult | None:
           if ctx.dry_run:
               # ... existing dry-run logic with reset count added ...
               ...

           def _send_reset_ccs() -> int:
               # ... as defined above ...

           def _send_ccs() -> int:
               # ... unchanged ...

           reset_sent = _send_reset_ccs()
           if reset_sent:
               logger.info("Sent %d reset CCs to %s", reset_sent, action.device)

           cc_sent = _send_ccs()
           ...

   The key change: `_send_reset_ccs()` is called BEFORE `_send_ccs()`.

5. **Edge cases to handle**:
   - `ctx.rig` is None (no rig loaded): `_send_reset_ccs` returns 0 silently
   - `device.config.controls` is empty list: `_send_reset_ccs` returns 0 silently
   - All controls have `default=None`: `_send_reset_ccs` returns 0 silently
   - `ctx.midi` is None (no MIDI connection): `_send_reset_ccs` returns 0 silently
   - `action.device` not found in rig: `_find_device` returns None, `_send_reset_ccs` returns 0

6. **Do not change `_send_ccs()`** — leave the preset CC send function exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run python -c "
from rig_chasebliss.applier import ChaseBlissApplier
from rig_chasebliss.applier import _find_device
from rig.engine.appliers.base import ApplyContext
# Verify _find_device exists and callable
assert callable(_find_device), '_find_device is callable'
# Verify applier class exists and has _build_preset
assert hasattr(ChaseBlissApplier, '_build_preset'), '_build_preset exists'
print('Applier structure OK')
"
</automated>
  </verify>
  <done>
    _build_preset sends reset CC batch (filtered to controls with non-None defaults) before
    preset CC batch; dry-run displays reset count; individual failures logged but non-blocking.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write reset-to-defaults tests</name>
  <files>packages/rig-chasebliss/tests/test_applier.py</files>
  <read_first>
    - packages/rig-chasebliss/src/rig_chasebliss/applier.py (full file — understand the
      public API: ChaseBlissApplier, _build_preset, _find_device)
    - packages/rig-chasebliss/src/rig_chasebliss/models.py (CbaSetupAction — what we
      construct for test actions)
    - packages/rig-chasebliss/src/rig_chasebliss/device.py (ChaseBlissConfig)
    - packages/rig-chasebliss/src/rig_chasebliss/catalog.py (Control, ControlType — for
      building test controls with defaults)
    - packages/rig-chasebliss/tests/test_device.py (test patterns in this package)
    - packages/rig/tests/engine/test_plan.py (look for ApplyContext construction patterns)
  </read_first>
  <action>
Create `packages/rig-chasebliss/tests/test_applier.py` with behavioral tests for the
reset-to-defaults feature. Use plain pytest functions.

Create helper fixtures/factories inline (no conftest.py required):

    from __future__ import annotations

    from unittest.mock import MagicMock

    from rig.engine.appliers.base import ApplyContext
    from rig.engine.state import RigState
    from rig.models.rig import Rig
    from rig_chasebliss.applier import ChaseBlissApplier, _find_device
    from rig_chasebliss.catalog import Control, ControlType
    from rig_chasebliss.device import ChaseBlissConfig
    from rig_chasebliss.models import CbaSetupAction

    def _make_rig(controls: list[Control]) -> Rig:
        from rig.models.rig import RigDevice
        device = RigDevice(
            id="test_cba",
            name="Test CBA",
            type="digital",
            config=ChaseBlissConfig(controls=controls),
            presets=[],
        )
        return Rig(
            name="test",
            devices=[device],
            scenes={},
            midi_channel=1,
        )

    def _make_ctx(rig: Rig, dry_run: bool = False) -> ApplyContext:
        midi = MagicMock() if not dry_run else None
        return ApplyContext(
            state=RigState(),
            rig=rig,
            dry_run=dry_run,
            confirmation_io=None,
            midi=midi,
            connected_devices={"test_cba"} if not dry_run else set(),
            config_path=None,
        )

    def _make_action() -> CbaSetupAction:
        return CbaSetupAction(
            device="test_cba",
            midi_channel=1,
            type="build_preset",
            preset_id="test_preset",
            preset_name="Test Preset",
            preset_number=1,
            cc_params=[{"cc": 14, "value": 100}],
        )

Test functions:

1. `test_reset_sent_before_cc_params` — Create a rig with controls that have defaults (e.g.,
   a KNOB with default=64). Create a ctx with a mock MIDI. Call _build_preset. Assert that
   `midi.send_control_change` was called with (device, cc, default, channel) BEFORE it was
   called with (device, cc, preset_value, channel) for any overlapping CC. Use mock_calls
   to check call order.

2. `test_reset_excludes_default_none_controls` — Create a rig with a mix of controls: one
   KNOB with default=64 (resettable), one SWITCH with default=None (excluded). Call
   _build_preset with live ctx. Assert that only 1 reset CC was sent (the KNOB), not the switch.
   Check that `midi.send_control_change.call_count` equals reset_count + cc_params_count.

3. `test_dry_run_shows_reset_count` — Create a rig with 3 resettable controls. Call
   _build_preset with dry_run=True. Assert it returns DeviceApplyResult with status="skipped".
   No need to assert console output — the dry-run path should not crash and returns correctly.

4. `test_no_resettable_controls_no_reset` — Create a rig where all controls have
   default=None. Call _build_preset live. Assert only the preset CCs were sent (call count
   matches len(cc_params)), and no additional CCs for reset were fired.

5. `test_reset_failure_non_blocking` — Create a rig with resettable controls. Set
   midi.send_control_change to raise Exception on the first call, then succeed. Call
   _build_preset. Assert that the method doesn't raise and returns status="confirmed".
   (The exception is caught and logged; preset CCs still go through.)

6. `test_find_device_returns_none_for_unknown_id` — Call `_find_device` with a rig that
   doesn't have the device. Assert it returns None.

7. `test_reset_per_preset_frequency` — Create two separate presets (two calls to
   _build_preset with different action data). After each call, verify that reset CCs were
   sent each time (not just the first). This tests unconditional per-preset behavior (D-02).

8. `test_reset_with_empty_controls` — Create a rig with controls=[] (empty list).
   Call _build_preset. Assert no errors, preset CCs still sent.

Use pytest parametrize where appropriate. Do not use conftest.py.

Imports at top of file should include all needed types. Use `from __future__ import annotations`
for forward references if needed.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig-chasebliss/tests/test_applier.py -v</automated>
  </verify>
  <done>
    All 8+ test functions pass; pytest reports 0 failures in test_applier.py.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full test suite green check</name>
  <files>(none — verification only)</files>
  <read_first>(none)</read_first>
  <action>
Run the full rig-chasebliss test suite to confirm no regressions:

    uv run pytest packages/rig-chasebliss/ -q

If any test fails, read the failure output carefully:
- A TypeError on `_send_reset_ccs` usually means the function signature is wrong or it's
  accessing a variable not in scope (check closure captures).
- An AssertionError in test_applier.py means the reset logic or mock setup is wrong.
- An AssertionError in test_device.py (if existing) means a regression — check if any
  import changes in applier.py caused side effects.
- An AttributeError on `device.config.controls` means the rig device config type doesn't
  match expectations.
- An AssertionError about call order means mock_calls ordering needs adjustment.

Do not modify existing tests in test_device.py or test_catalog.py.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && uv run pytest packages/rig-chasebliss/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    pytest exits 0; output ends with N passed (no failures, no errors).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applier.py reset code | Computes reset CCs from hardcoded catalog defaults (device.config.controls); no external/user input path |
| control.default values | Sourced from official CBA PDF manuals, committed to catalog.py; immutable at runtime |
| MIDI output path | Reset CCs sent via same midi.send_control_change path as preset CCs — no new I/O surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-16-01 | Tampering | control.default values | accept | Hardcoded in catalog.py from official manuals; no user-controlled input for defaults |
| T-16-02 | DoS | Large reset batch (many controls) | accept | Max ~47 CCs for Mood MkII — well within MIDI buffer capacity |
| T-16-03 | DoS | MIDI send failure during reset | accept | Individual failures are caught and logged; no cascade to preset CC send or state persistence |
| T-16-04 | Spoofing | Reset sends CC messages with value=int(default) | accept | `int(default)` is safe for float None due to the `is not None` filter; no type confusion possible |
</threat_model>

<verification>
After all tasks complete, verify end-to-end correctness:

1. `uv run pytest packages/rig-chasebliss/ -q` — exits 0, all tests pass.
2. `grep -c "_send_reset_ccs" packages/rig-chasebliss/src/rig_chasebliss/applier.py` — returns at least 2 (definition + call site).
3. `grep -c "_find_device" packages/rig-chasebliss/src/rig_chasebliss/applier.py` — returns at least 2 (definition + call site).
4. Run: `uv run python -c "from rig_chasebliss.applier import _find_device; assert callable(_find_device)"` — no import error.
5. `grep -c "default is not None" packages/rig-chasebliss/src/rig_chasebliss/applier.py` — returns at least 1 (the filter in reset logic).
6. `grep -c "default=None" packages/rig-chasebliss/src/rig_chasebliss/applier.py` — returns 0 (these are excluded, no spurious sends).
</verification>

<success_criteria>
- `_build_preset` sends reset CC batch before preset CC batch on every invocation.
- Reset batch includes only controls where `control.default is not None and control.midi_cc is not None`.
- Footswitch and utility controls (`default=None`) are excluded — no CC messages sent for them.
- Dry-run prints reset count alongside existing CC param output.
- Individual CC send failures during reset are logged and do not block preset CCs.
- `uv run pytest packages/rig-chasebliss/ -q` exits 0 with no failures.
</success_criteria>

<output>
Create `.planning/phases/16-reset-to-defaults/16-01-SUMMARY.md` when done.
</output>
