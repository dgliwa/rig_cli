---
phase: 25-io-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rig/src/rig/interaction/midi.py
  - packages/rig-analog/src/rig_analog/device.py
  - packages/rig-analog/src/rig_analog/interaction.py
  - packages/rig-analog/src/rig_analog/__init__.py
  - packages/rig/tests/test_devices.py
autonomous: true
requirements:
  - IO-01
  - IO-02

must_haves:
  truths:
    - "rig apply with an analog device never calls builtins.input in any applier"
    - "Three analog tests use InMemoryPromptAdapter without monkeypatching builtins.input"
    - "make test passes with no regressions"
    - "prompt_analog() is deleted — no references remain in non-test files"
  artifacts:
    - path: "packages/rig/src/rig/interaction/midi.py"
      provides: "prompt_device() with correct non-MIDI display when midi_channel is None"
      contains: "Set {device} manually"
    - path: "packages/rig-analog/src/rig_analog/device.py"
      provides: "AnalogDevice.apply() using ctx.confirmation_io.prompt_device() with retry loop"
      contains: "confirmation_io"
    - path: "packages/rig-analog/src/rig_analog/__init__.py"
      provides: "Clean __init__.py with no prompt_analog export"
  key_links:
    - from: "packages/rig-analog/src/rig_analog/device.py"
      to: "ctx.confirmation_io.prompt_device()"
      via: "DeviceApplyContext.confirmation_io"
      pattern: "confirmation_io\\.prompt_device"
    - from: "packages/rig/tests/test_devices.py"
      to: "InMemoryPromptAdapter"
      via: "_make_apply_ctx(confirmation_io=...)"
      pattern: "InMemoryPromptAdapter"
---

<objective>
Thread ConfirmationIO through AnalogDevice.apply() — eliminating the last raw input() call in an applier path. After this plan executes, all device confirmation prompts go through the ConfirmationIO Protocol and analog tests use InMemoryPromptAdapter without monkeypatching builtins.input.

Purpose: IO parity across all device types so the apply engine is fully testable without stdin capture.
Output: Updated AnalogDevice.apply() using ctx.confirmation_io.prompt_device(), deleted prompt_analog(), fixed prompt_device() display for analog, and 3 migrated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/rig/src/rig/engine/ports.py
@packages/rig/tests/fakes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix prompt_device() display for analog (midi_channel=None)</name>
  <files>packages/rig/src/rig/interaction/midi.py</files>
  <behavior>
    - When midi_channel is None and midi_connected is False, prompt_device() prints "→ Set {device} manually (knobs/switches)" instead of "→ Connect MIDI to {device}"
    - When midi_channel is not None, the original "→ Connect MIDI to {device}" message continues to display (no regression)
    - When midi_connected is True, the "Sent via MIDI" confirmation path is unaffected
  </behavior>
  <action>
    In packages/rig/src/rig/interaction/midi.py, locate the prompt_device() function (around line 32). The current unconditional line:

      console.print(f"[dim]→ Connect MIDI to {device}[/dim]")

    Replace the else branch so it is conditional on midi_channel:

      if midi_channel is None:
          console.print(f"[dim]→ Set {device} manually (knobs/switches)[/dim]")
      else:
          console.print(f"[dim]→ Connect MIDI to {device}[/dim]")

    The change is inside the `else` block that fires when midi_connected is False. Do not alter the if-branch that prints the green "Sent via MIDI" confirmation, the header line, the while loop, or any other logic. Per D-02.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && grep -n "Set.*manually" packages/rig/src/rig/interaction/midi.py</automated>
  </verify>
  <done>packages/rig/src/rig/interaction/midi.py contains the "Set {device} manually (knobs/switches)" branch guarded by `if midi_channel is None`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor AnalogDevice.apply() to use ctx.confirmation_io with retry loop</name>
  <files>packages/rig-analog/src/rig_analog/device.py</files>
  <behavior>
    - apply() calls ctx.confirmation_io.prompt_device(action.device, action.preset_name, None, None, False) instead of prompt_analog()
    - When prompt_device() returns "retry", apply() re-calls prompt_device() in a loop until "confirm", "skip", or "quit" is returned
    - "confirm" → update_device_state + DeviceApplyResult(status="confirmed")
    - "quit" → DeviceApplyResult(status="error", error="quit")
    - "skip" → DeviceApplyResult(status="skipped")
    - dry_run path is unchanged
    - No import of prompt_analog remains in device.py
  </behavior>
  <action>
    In packages/rig-analog/src/rig_analog/device.py:

    1. Remove the import: `from rig_analog.interaction import prompt_analog`

    2. Replace the apply() body after the dry_run guard with a retry loop. The new logic (after the dry_run early return):

      while True:
          res = ctx.confirmation_io.prompt_device(
              action.device, action.preset_name, None, None, False
          )
          if res == "quit":
              return DeviceApplyResult(
                  device=action.device, status="error", preset=action.preset_name, error="quit"
              )
          if res == "confirm":
              update_device_state(ctx.state, action.device, last_preset=action.preset_name)
              return DeviceApplyResult(
                  device=action.device, status="confirmed", preset=action.preset_name
              )
          if res == "retry":
              continue
          return DeviceApplyResult(
              device=action.device, status="skipped", preset=action.preset_name
          )

    Per D-01 and D-03. Do not change imports for anything other than removing prompt_analog. Do not add a max-retry limit — match the unbounded loop behavior of the original prompt_analog() which looped on invalid input.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && grep -n "confirmation_io" packages/rig-analog/src/rig_analog/device.py && ! grep -n "prompt_analog" packages/rig-analog/src/rig_analog/device.py</automated>
  </verify>
  <done>packages/rig-analog/src/rig_analog/device.py uses ctx.confirmation_io.prompt_device() in a while-True loop; prompt_analog import is absent</done>
</task>

<task type="auto">
  <name>Task 3: Delete prompt_analog() and clean __init__.py</name>
  <files>
    packages/rig-analog/src/rig_analog/interaction.py,
    packages/rig-analog/src/rig_analog/__init__.py
  </files>
  <action>
    Per D-04:

    1. Delete the file packages/rig-analog/src/rig_analog/interaction.py entirely. It contains only the prompt_analog() function which is now dead code.

    2. In packages/rig-analog/src/rig_analog/__init__.py, remove both lines:
       - `from rig_analog.interaction import prompt_analog`
       - `"prompt_analog",` from __all__

    After cleanup, __init__.py should be empty or contain only the remaining __all__ = [] if needed. If removing those two lines leaves __init__.py empty, that is acceptable — leave an empty file rather than deleting it (Python package marker).

    Confirm no other file in packages/rig-analog/src/ imports prompt_analog before deleting:
      grep -r "prompt_analog" packages/rig-analog/src/
    The only result should be the __init__.py line (which you will also remove). If any other file imports it, fix that import first.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! test -f packages/rig-analog/src/rig_analog/interaction.py && ! grep -r "prompt_analog" packages/rig-analog/src/</automated>
  </verify>
  <done>interaction.py is deleted; no reference to prompt_analog exists in packages/rig-analog/src/</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Migrate 3 analog tests from patch() to InMemoryPromptAdapter</name>
  <files>packages/rig/tests/test_devices.py</files>
  <behavior>
    - test_analog_device_apply_confirm_returns_confirmed: result.status == "confirmed" and result.preset == "Noon" using InMemoryPromptAdapter(default="confirm")
    - test_analog_device_apply_quit_returns_error: result.status == "error" and result.error == "quit" using InMemoryPromptAdapter(default="quit")
    - test_analog_device_apply_skip_returns_skipped: result.status == "skipped" and result.device == "fuzz" using InMemoryPromptAdapter(default="skip")
    - No test in the file uses patch("rig_analog.device.prompt_analog", ...) after migration
    - If `from unittest.mock import patch` is only used by those 3 tests, remove the import from those test functions
  </behavior>
  <action>
    Per D-05. Migrate exactly 3 test functions in packages/rig/tests/test_devices.py:

    **test_analog_device_apply_confirm_returns_confirmed** (line ~140):
    Remove `from unittest.mock import patch` inside the function body.
    Remove `with patch("rig_analog.device.prompt_analog", return_value="confirm"):` context manager.
    Add `confirmation_io=InMemoryPromptAdapter(default="confirm")` to the _make_apply_ctx() call.
    The assertion lines (result.status == "confirmed", result.preset == "Noon") are unchanged.

    **test_analog_device_apply_quit_returns_error** (line ~153):
    Same pattern — remove patch, add `confirmation_io=InMemoryPromptAdapter(default="quit")`.
    Assertions (result.status == "error", result.error == "quit") are unchanged.

    **test_analog_device_apply_skip_returns_skipped** (line ~343):
    Same pattern — remove patch, add `confirmation_io=InMemoryPromptAdapter(default="skip")`.
    Assertions (result.status == "skipped", result.device == "fuzz") are unchanged.

    After migration, scan the file: if `from unittest.mock import patch` no longer appears in any test body or at module level, it is already absent at module level (the file uses MagicMock from unittest.mock). Check with:
      grep -n "patch" packages/rig/tests/test_devices.py
    Remove any remaining `from unittest.mock import patch` lines that are now orphaned.

    InMemoryPromptAdapter is already imported at the top of test_devices.py (`from tests.fakes import InMemoryPromptAdapter`). Do not add a duplicate import.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && ! grep -n "patch.*prompt_analog" packages/rig/tests/test_devices.py && uv run pytest packages/rig/tests/test_devices.py::test_analog_device_apply_confirm_returns_confirmed packages/rig/tests/test_devices.py::test_analog_device_apply_quit_returns_error packages/rig/tests/test_devices.py::test_analog_device_apply_skip_returns_skipped -v</automated>
  </verify>
  <done>All 3 migrated tests pass using InMemoryPromptAdapter; no patch("rig_analog.device.prompt_analog") call remains in the file</done>
</task>

<task type="auto">
  <name>Task 5: Full test suite verification and input() audit</name>
  <files></files>
  <action>
    Run the full test suite and verify the input() elimination invariant:

    1. Run: make test
       All tests must pass. If any test fails, investigate and fix before proceeding.

    2. Audit for raw input() calls in applier paths:
       grep -rn "input(" packages/*/src/
       Expected: Only RichConfirmationIO.prompt_mc6_navigate() in packages/rig/src/rig/engine/ports.py and the prompt_midi_connect() helper in packages/rig/src/rig/interaction/midi.py should contain input() calls. No applier (device.py file in any plugin package) should call input() directly.

    3. Confirm prompt_analog is gone:
       grep -rn "prompt_analog" packages/
       Expected: zero results (all references removed).

    If any unexpected input() call is found in an applier (device.py under packages/rig-*/src/), fix it before marking this task done.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig-cli && make test && ! grep -rn "prompt_analog" packages/ && ! grep -rn "input(" packages/rig-analog/src/ packages/rig-chasebliss/src/ packages/rig-morningstar/src/ packages/rig-hx/src/ 2>/dev/null</automated>
  </verify>
  <done>make test passes; prompt_analog is absent from all packages; no input() call exists in any plugin applier package src directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → terminal input | All user keystrokes pass through ConfirmationIO adapters; no direct stdin in appliers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | InMemoryPromptAdapter in tests | accept | Test-only fake; not reachable in production; ConfirmationIO Protocol ensures production adapter is wired at CLI entrypoint |
| T-25-02 | Denial of Service | Infinite retry loop in AnalogDevice.apply() | accept | Loop exits on "confirm", "skip", or "quit"; retry is a deliberate user action; no automated path triggers it |
</threat_model>

<verification>
1. `make test` passes with no failures
2. `grep -rn "prompt_analog" packages/` returns zero results
3. `grep -rn "input(" packages/rig-analog/src/` returns zero results
4. `grep -n "confirmation_io" packages/rig-analog/src/rig_analog/device.py` returns a match
5. `grep -n "Set.*manually" packages/rig/src/rig/interaction/midi.py` returns a match
</verification>

<success_criteria>
1. `rig apply` with an analog device never calls builtins.input in any applier — confirmed by grep showing no input() in plugin src directories
2. Three migrated tests (confirm/quit/skip) pass using InMemoryPromptAdapter(default=...) with no patch() calls
3. `make test` passes with no regressions across all packages
4. prompt_analog() is deleted — interaction.py removed, __init__.py cleaned, zero grep hits in packages/
5. prompt_device() in rig.interaction.midi shows "Set {device} manually (knobs/switches)" when midi_channel is None
</success_criteria>

<output>
Create `.planning/phases/25-io-parity/25-01-SUMMARY.md` when done
</output>
