---
milestone: v1.1
audited: 2026-06-07
status: passed
scores:
  requirements: 8/8
  phases: 2/2
  integration: 2/2
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "06-core-package-entry-point-discovery"
    items:
      - "VERIFICATION.md not created during execution (pi execute-phase workflow does not generate it)"
  - phase: "07-plugin-package-wiring"
    items:
      - "VERIFICATION.md not created during execution (pi execute-phase workflow does not generate it)"
  - deferred:
    - "BUILD-01: Independent versioning per package (Phase 8 — removed from roadmap)"
    - "BUILD-02: Editable installs CI check (Phase 8)"
    - "BUILD-03: CI publish pipeline (Phase 8)"
    - "DOCS-01: Plugin authoring guide (Phase 8)"
---

# v1.1 Milestone Audit — Package Extraction & Plugin Isolation

**Status: PASSED** ✓

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| CORE-01: `rig.devices` entry point group | Phase 6 | **satisfied** | `packages/rig/pyproject.toml` declares `[project.entry-points."rig.devices"]` |
| CORE-02: Runtime discovery via entry points | Phase 6 | **satisfied** | `get_registry()` in `plugin_registry.py` uses `importlib.metadata.entry_points(group="rig.devices")` |
| CORE-03: Migration from PluginRegistry to entry points | Phase 6 | **satisfied** | `default_registry` and all `register()` calls removed; `reload_registry()` exposed |
| CORE-04: Zero hard deps on device plugins | Phase 6 | **satisfied** | `packages/rig/pyproject.toml` has zero `[project.dependencies]` on `rig-*` packages |
| ANLG-01: rig-analog separate package | Phase 7 | **satisfied** | Entry point `manual → rig_analog.device:AnalogDevice` |
| CHASE-01: rig-chasebliss with device-level MIDI | Phase 7 | **satisfied** | Entry point `chase_bliss → rig_chasebliss.device:ChaseBlissDevice`; `setup()` manages MIDI connection + 3-phase init |
| MC6-01: rig-morningstar with device-level MIDI | Phase 7 | **satisfied** | Entry point `controller → rig_morningstar.device:MC6Device`; `setup()` manages MIDI connection |
| HX-01: rig-hx separate package | Phase 7 | **satisfied** | Entry point `midi → rig_hx.device:HXStompDevice`; full Device Protocol implementation |

**Total: 8/8 requirements satisfied**

## Phase Verification Status

| Phase | Plans | VERIFICATION.md | Status |
|-------|-------|-----------------|--------|
| 6. Core Package & Entry Point Discovery | 2/2 | Missing | ⚠ Not generated (workflow gap) |
| 7. Plugin Package Wiring & Device-Level MIDI | 3/3 | Missing | ⚠ Not generated (workflow gap) |

**Note:** VERIFICATION.md files were not created by the pi-based execute-phase workflow.
Actual requirements verification was performed via code inspection and test results.

## Cross-Phase Integration

| Check | Result |
|-------|--------|
| Entry points resolve to correct device classes | ✓ `manual→AnalogDevice`, `midi→HXStompDevice`, `chase_bliss→ChaseBlissDevice`, `controller→MC6Device` |
| `reload_registry()` discovers all 4 plugins | ✓ All 4 models load from entry points |
| Device Protocol structural conformance | ✓ All 4 classes have `setup`, `apply`, `get_scene_pc_command`, `presets` |
| `apply_plan()` works without `midi_connection_io` | ✓ Parameter removed, tests pass |
| Engine has no hard plugin imports | ✓ Zero `import rig-*` in engine code |
| Old `rig.engine.devices.py` deleted | ✓ No stale imports remain |
| `setup()` handles `midi=None` gracefully | ✓ HXStompDevice and ChaseBlissDevice have guards for `dry_run` and `midi is None` |

## End-to-End Flows

| Flow | Result |
|------|--------|
| Entry point discovery → device loading → plan → apply | ✓ 302 tests passing |
| Plugin-less operation (no plugins installed) | ✓ Empty registry = graceful degradation |
| MIDI device setup + PC send | ✓ HXStompDevice.setup() connects MIDI, apply() sends PC |
| CBA 3-phase setup (channel → preset → scene) | ✓ ChaseBlissDevice.setup() runs full init via applier |
| Analog non-MIDI device | ✓ No MIDI connection attempted; instructional prompts |

## Deferred Items

| Item | Reason |
|------|--------|
| BUILD-01: Independent versioning | Phase 8 — removed from roadmap |
| BUILD-02: Editable installs CI | Phase 8 — removed from roadmap |
| BUILD-03: CI publish pipeline | Phase 8 — removed from roadmap |
| DOCS-01: Plugin authoring guide | Phase 8 — removed from roadmap |
| VERIFICATION.md files | Not generated by pi GSD workflow (cosmetic gap — no actual verification issues) |

## Nyquist Compliance

Nyquist validation files were not created during execution (pi-based workflow does not generate `*-VALIDATION.md`). Discovery only — no action required.

## Summary

**PASSED.** All 8 v1.1 requirements satisfied across Phases 6-7. Cross-phase integration verified. No critical blockers. Accumulated tech debt is limited to deferred Phase 8 items (removed from roadmap) and missing VERIFICATION.md files (workflow artifact gap, not a quality gap).

302 tests passing. Ruff clean. Project is ready for next milestone.
